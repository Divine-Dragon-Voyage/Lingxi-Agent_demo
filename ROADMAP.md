# 灵犀智能体原型开发路线

## 2026-09-18 工作流与客服配置体验优化（已完成）

- 左侧导航隐藏「团队」入口，保留历史团队路由与数据，不影响既有实体引用。
- 工作流列表新增「创建时间」「状态」列，状态从名称列移出；操作栏收敛为更多菜单；搜索范围收敛为工作流名称，搜索占位符同步改为「搜索工作流名称」。
- AI 客服详情页的绑定工作流弹窗改为多选绑定，仅展示已发布工作流，列表行按「复选框 + 图标 + 流程名称」呈现，底部按钮改为「绑定」。
- AI 客服详情页的绑定知识库弹窗改为多选绑定，列表样式与绑定工作流弹窗保持一致，底部新增「绑定」按钮。
- 工作流编排页改为不显示左侧菜单栏的全屏二级页面，顶部导航对齐 AI 客服详情页样式，包含发布状态、最近保存、保存状态与发布按钮。
- 全局补齐详情区最后一个字段不显示底部分割线的样式约束，避免页面尾部出现多余横线。
- 收件箱会话详情页同时展示「上一个会话」与「下一个会话」状态，不再只展示单向切换状态。
- 验证通过：`npm run build`；Playwright 本地预览核验工作流列表、绑定工作流弹窗、绑定知识库弹窗、工作流全屏编排页和收件箱会话上下切换状态。

## 2026-09-18 GitHub Pages 部署准备（已完成）

- 已确认项目技术栈为 React + TypeScript + Vite + Arco Design，构建命令为 `npm run build`。
- 已完成公开前敏感信息扫描：未发现 `.env`、真实 API Key、Token、密码或私钥；代码中的 `Access Token` 仅为渠道配置模拟表单字段。
- 已补充 GitHub Pages 自动部署工作流 `.github/workflows/deploy-pages.yml`，使用 GitHub Actions 构建 `dist` 并部署 Pages。
- 已修正 GitHub Pages 兼容配置：Vite 根据 `GITHUB_REPOSITORY` 自动设置 `base`，路由切换为 `HashRouter`，公开目录静态资源改为跟随 `import.meta.env.BASE_URL`。
- 已更新 `.gitignore`，排除本地 Playwright、浏览器 profile 和输出缓存，避免将本地验证数据新增进公开仓库。
- 本地验证通过：`npm install`、`npm run build`、带 `GITHUB_REPOSITORY=Divine-Dragon-Voyage/Lingxi-Agent_demo` 的 Pages 构建、Vite 生产预览；Playwright 验证首页、知识库、渠道管理、静态资源和子页面刷新，控制台 0 错误。
- 已由用户完成 GitHub CLI 登录并确认公开发布，代码已推送到现有公开仓库 `Divine-Dragon-Voyage/Lingxi-Agent_demo`，GitHub Pages 已可公开访问。

## 2026-09-17 工作流画布与 AI Agent 绑定（已完成）

- 已确认增量需求记录于 `prd/workflow_demo_prd.md`，并在 `CLAUDE.md` 和当前 PRD 中明确其优先级；保留现有 Arco、设计 Token 与统一导航。
- `/workflows` 替换占位页：支持名称／触发器搜索、新建必填校验、名称旁发布状态、分页、复制草稿、编辑和二次确认删除。内置订单进度查询（已发布）与售后转人工（草稿）示例。
- `/workflows/:id` 使用项目级 `@xyflow/react`：可拖入或点击添加节点、移动、连线、编辑配置、删除节点／连线、缩放、适应画布和小地图；开始节点不可删除。包含开始、结束、AI 任务、信息收集、接口调用、条件分支、发送回复、转人工八种节点。
- 工作流经统一 reducer 自动保存，localStorage 刷新恢复；保留旧本地数据并为无工作流字段的历史数据补充示例。保存失败保留编辑内容、展示重试，未保存成功禁止发布。
- 草稿和发布快照隔离；发布校验名称、触发器、节点必填、JSON、团队引用、重复收集字段、连通性、分支出口和循环；可定位错误节点。发布成功后才可绑定，继续编辑不影响 Agent 使用的已发布内容。
- Agent 工作流页复用知识库绑定节奏，支持搜索、多选已发布工作流、查看发布版、确认解绑；操作写入 Agent 草稿，沿用 Agent 发布生效规则。被草稿或已发布 Agent 引用的工作流禁止删除，并列出引用者。
- Agent 查看工作流为只读发布快照，保留返回原 Agent 的路径；手机工作流页自动收起壳层侧栏，节点面板可收起，小地图在窄屏隐藏，避免遮挡画布控件。
- 验证通过：`npm.cmd run build`、`oxlint src`（保留既有 Fast Refresh 警告）、新增工作流源码严格 lint、`git diff --check`。默认 `npm run lint` 会扫描已有 `output` 浏览器缓存，已停止该扫描并以源码检查替代；未改动历史缓存。
- Playwright 实测通过：创建必填校验、无连线发布失败、添加／拖入／拖动节点、手动连线、节点和连线删除确认／取消、发布与刷新、草稿隔离、绑定发布版、只读查看、解绑确认／取消、已发布 Agent 引用保护、复制／删除、搜索、保存失败与重试、发布候选过滤。规则校验覆盖无效 JSON、缺分支、重复字段、孤立节点、循环及失效团队。
- 已检查 1440×1000 和 390×844 的列表、弹窗、画布与 Agent 页面截图，无页面横向溢出；修复 React Flow 尺寸未回传导致的拖拽警告，小地图节点正常渲染，回归无 React Flow 警告与页面未捕获异常。Arco 在 React 19 开发模式下仍有 `element.ref` 兼容提示，未扩大范围修改依赖。
- 回归脚本与截图：`output/playwright/workflow-check.js`、`workflow-edge-check.js`、`workflow-*.png`；测试使用隔离浏览器，不修改用户浏览器数据。本地服务 `http://localhost:5173/workflows` 已验证 HTTP 200。
- 演示边界：不请求真实业务接口、不执行真实 AI 或流程引擎，接口返回内容为可编辑的模拟 JSON。无新增待确认事项。

## 2026-09-17 设置模块与聊天超时配置（已完成）

- 侧边栏新增一级菜单「设置」（渠道后），路由 `/settings`；页面为左侧配置栏 + 右侧配置内容的双栏结构，复用渠道页节奏。
- 「设置」菜单项改为固定在侧边栏最底部，与参考产品一致。
- 配置栏含「收件箱」分组，标题右侧提供收起／展开图标，展开后显示「聊天超时」配置项。
- 聊天超时配置：默认勾选、默认 15 分钟；选项行为左侧文案「如果没有新消息，超过以下时长后关闭会话」（关闭会话加粗，对应原文 close chat）、右侧分钟数上下调节输入框（范围 1–1440）；取消勾选时输入框隐藏；副标题为「选择会话没有新消息时的处理方式。」。
- 说明文字与配置卡片在内容区水平居中；修改勾选或分钟数后底部出现悬浮置底的「取消／保存」操作条，水平居中并带上投影区分内容区；保存写入 store 并 toast 提示，取消恢复上次保存值；配置随 localStorage 持久化，旧数据自动补默认值。
- `npm run build` 通过；lint 仅保留既有 Fast Refresh 警告；Playwright 已实际核验菜单贴底、分组收起展开、内容居中、操作条置底居中、保存 toast、取消恢复和勾选联动。

## 2026-09-17 渠道列表状态列改名（已完成）

- 渠道类型列表表头「状态」改为「启用状态」，与列内启停开关语义一致。
- `npm run build` 通过。

## 2026-09-17 AI 客服详情页精简与预览测试升级（已完成）

- 个人资料隐藏团队字段（数据保留）；移除「记忆」和「转人工」配置区块，发布校验同步去掉转人工依赖。
- 回复语言选项调整为简体中文／繁体中文／英语／越南语／泰语／高棉语／缅甸语／老挝语，英语带「默认」标签并作为默认值；旧本地存储中的历史语言值在读取时无损归一为英语。
- 详情左侧菜单新增「工作流」（知识库后，当前为「暂无工作流」空态，后续绑定资源中心工作流）和「接待设置」（工作流后）；接待设置提供欢迎语开关，默认关闭，开启后出现输入框并支持 500 字计数。
- 预览与测试面板移除「测试设置」玩家 ID 配置；消息改用与收件箱一致的头像＋作者名＋气泡＋时间结构，客户消息在右、客服回复在左；输入框支持上传图片，标题右侧新增清除全部消息按钮。
- 修复预存的 Arco `Message` 等静态方法在 React 19 下失效问题：入口调用 `setCreateRoot` 注入 `react-dom/client` 的 `createRoot`，全站 toast 反馈恢复正常。
- `npm run build` 与 `oxlint` 通过；Playwright 已实际核验菜单四项、语言下拉、欢迎语开关交互、消息收发站位、图片上传与清除反馈。

## 2026-09-17 左侧菜单信息架构调整（已完成）

- 左侧一级菜单「AI 客服」调整为「AI Agents」，点击后直接进入原客服管理列表页 `/agents`。
- 新增一级菜单「资源中心」，下挂「知识库」和「工作流」两个子菜单；知识库继续复用原 `/knowledge` 页面。
- 新增 `/workflows` 工作流占位页，当前展示“工作流后续新增”的空状态，避免点击后空白。
- `npm.cmd run build` 通过；`oxlint src/App.tsx src/components/AppShell.tsx src/pages/ModulePages.tsx --max-warnings=0` 通过；`/agents`、`/knowledge`、`/workflows` 均返回 HTTP 200。

## 2026-09-17 渠道连接校验放宽（已完成）

- Telegram 新渠道连接校验调整为基础格式校验：`数字:非空 token` 即可模拟连接成功，不再要求冒号后至少 8 位。
- 保留 `fail` / `invalid` 关键词触发失败态，方便演示异常校验场景。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx --max-warnings=0` 通过；`/channels/telegram/new` 返回 HTTP 200。

## 2026-09-17 收件箱标题分割线与 AI 客服头像同步（已完成）

- 去掉收件箱左侧栏大标题「收件箱」与下方「聊天」会话类型之间的分割线。
- 处理中 AI 客服列表头像改为从 AI Agent 实体同步，优先展示已配置头像；无头像时保留机器人兜底图标。
- 筛选浮层中的 AI Agent 头像同步使用同一套头像来源，避免同一页面展示不一致。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/styles/global.css --max-warnings=0` 通过；`/inbox?status=processing&conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-17 渠道空状态搜索框隐藏（已完成）

- 渠道类型列表为空时不再展示「搜索渠道名称」搜索框，仅保留空状态插画、文案和添加新连接按钮。
- 渠道已有连接但搜索无结果时继续保留搜索框，方便用户清空或调整关键词。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx --max-warnings=0` 通过；`/channels/livechat`、`/channels/telegram` 均返回 HTTP 200。

## 2026-09-17 渠道启停反馈提示修复（已完成）

- 渠道列表启停开关不再只依赖全局 `Message`，新增渠道页内部固定反馈提示条，点击启用／停用后会在页面顶部中间稳定显示「渠道已启用」或「渠道已停用」。
- 未配置接待 AI Agent 时阻止启用，并在同一反馈提示条展示「请先配置接待 AI Agent 后再启用渠道」。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/styles/global.css --max-warnings=0` 通过；`/channels/livechat`、`/channels/telegram` 均返回 HTTP 200。

## 2026-09-17 渠道接待 AI Agent 下拉修复（已完成）

- 将渠道添加／编辑弹窗中的「接待 AI Agent」从 Arco Select 浮层改为弹窗内部自定义下拉面板，点击选择框后在当前字段下方展开 Agent 列表，不再依赖浮层挂载和 z-index。
- 下拉选项继续展示 AI 客服圆形头像与名称，选中后同步写入渠道配置草稿。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/styles/global.css --max-warnings=0` 通过；`/channels/livechat/new`、`/channels/telegram/new` 均返回 HTTP 200。

## 2026-09-17 渠道搜索框与按钮文字修正（已完成）

- 渠道列表搜索框外层工具栏与 Arco 输入组件均强制收敛为 244px，避免被父级容器拉伸成通栏。
- 「添加新连接」按钮及其内部图标、文字统一强制为白色。
- `npm.cmd run build` 通过；源码断言确认 `.channel-list-toolbar` 与 `.channel-add-button` 样式已落地；`/channels/livechat`、`/channels/telegram` 均返回 HTTP 200。

## 2026-09-17 渠道页面第二轮细节修复（已完成）

- 渠道栏与渠道内容栏标题高度收敛到 56px，与其他菜单栏标题节奏保持一致，并缩小标题与内容的首屏间距。
- 渠道列表搜索框继续保持与知识库页面一致的 244px 宽度；列表区域保持左右 24px 内容边距。
- 渠道启停开关继续触发成功／警告消息提示；未配置接待 AI Agent 时阻止启用并提示原因。
- 「添加新连接」按钮文字改为白色，避免主按钮文字呈灰色。
- 渠道添加／编辑弹窗回归通用弹窗标题尺寸，移除标题下方描述文案；标题仍保留渠道 Logo +「接入／编辑」+ 渠道名称。
- 接待 AI Agent 下拉选择器弹层改为挂载到 `document.body`，并补充弹层 z-index，避免被弹窗容器遮挡导致点击无反应。
- 渠道卡片描述与按钮之间补充间距，未连接按钮改为更小的 32px 白底描边按钮。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/styles/global.css --max-warnings=0` 通过；`/channels`、`/channels/livechat`、`/channels/telegram`、`/channels/livechat/new` 均返回 HTTP 200。

## 2026-09-17 渠道列表名称列精简（已完成）

- 渠道类型管理页的「渠道名称」列去掉渠道 Logo，仅展示渠道名称文本；左侧渠道菜单、全部渠道卡片和接入弹窗标题中的渠道 Logo 保持不变。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx --max-warnings=0` 通过。

## 2026-09-17 渠道页面视觉细节修复（已完成）

- 渠道列表搜索框改为与知识库页面一致的 244px 宽度，并让列表区域保留左右 24px 内容边距，不再通栏贴边。
- 修复「添加新连接」按钮文字掉字问题，按钮保持单行展示「+ 添加新连接」。
- 接待 AI Agent 字段头像改为圆形，并继续读取 AI 客服实体头像，保证与 AI 客服模块同步。
- 渠道栏标题高度与内容区标题统一为 72px；缩小标题与菜单间距；左侧内菜单「渠道」改为「全部」。
- 渠道接入弹窗标题改为「渠道 Logo + 接入/编辑 + 渠道名称」，字段与输入框改为上下布局，并移除重复关闭按钮。
- 全部渠道卡片改为一行三列比例，未连接按钮调整为白底描边样式。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/styles/global.css --max-warnings=0` 通过；`/channels`、`/channels/livechat`、`/channels/telegram`、`/channels/livechat/new` 均返回 HTTP 200。

## 2026-09-17 渠道模块二轮高保真优化（已完成）

- 渠道内页结构优化：左侧渠道标题与渠道类型归入同一栏，右侧内容区用分割线隔开；左侧「全部渠道」改为「渠道」，右侧仍显示「全部渠道」。
- 全部渠道卡片改为更接近 Text 风格的大圆角卡片，并将描述文案调整为中文：「Livechat 消息会以会话形式进入你的收件箱。」和「Telegram 消息会以会话形式进入你的收件箱。」。
- 渠道列表新增渠道名称搜索；表格字段调整为「渠道名称 / 接待 AI Agent / 创建时间 / 状态 / 操作」，接待 AI Agent 由头像与名称组成，状态改为开关并补齐启停消息提示。
- 渠道配置从全页改为弹窗形式，字段名称中文化，保留 Access Token、Bot Token、Webhook URL 等必要英文技术字段；新建与编辑继续模拟连接校验。
- 列表更多菜单仅保留「编辑、删除」；删除渠道改为输入渠道名称二次确认，文案说明入站消息停止、历史会话保留。
- 渠道类型无数据时在列表区展示 PNG 空状态插画与引导文案，插画资产保存为 `src/assets/channel-empty-state.png`。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/styles/global.css src/App.tsx src/components/AppShell.tsx --max-warnings=0` 通过；`/channels`、`/channels/livechat`、`/channels/telegram`、`/channels/livechat/new` 均返回 HTTP 200。

## 2026-09-17 渠道子页面空白修复（已完成）

- 修复渠道模块子路由解析错误：`/channels/livechat`、`/channels/telegram`、`/channels/livechat/new` 现在会正确识别渠道类型与页面模式，不再进入空白页面。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx --max-warnings=0` 通过；`/channels`、`/channels/livechat`、`/channels/telegram`、`/channels/livechat/new` 均返回 HTTP 200。

## 2026-09-17 渠道模块高保真 Demo 新增（已完成）

- 左侧导航新增一级「渠道」入口并排在最后，路由切换到新的 `/channels/*` 渠道模块页面。
- 新增「全部渠道」卡片页，支持 LiveChat 与 Telegram 两类渠道，展示描述、已连接数量，并根据连接状态提供「连接／管理」入口。
- 新增 LiveChat / Telegram 渠道类型管理页，按创建时间倒序展示渠道名称、AI Agent、状态和更多操作，支持编辑、启用、停用、删除。
- 新增渠道配置页：LiveChat 支持 Channel Name、Access Token、Webhook URL、AI Agent；Telegram 支持 Channel Name、Bot Token、AI Agent；新增用「连接」，编辑用「保存」且未修改时置灰。
- 删除渠道改为输入渠道名称二次确认；删除仅移除渠道配置，保留历史会话数据；删除 AI Agent 后渠道保留、AI Agent 显示未配置并自动停用，未配置 AI Agent 的渠道不能直接启用。
- Mock 数据补充 Telegram 渠道并兼容旧 LiveChat 渠道数据；渠道数据继续使用 localStorage 持久化。
- `npm.cmd run build` 通过；`oxlint src/pages/ChannelsPage.tsx src/components/AppShell.tsx src/App.tsx src/types.ts src/mockData.ts src/styles/global.css --max-warnings=0` 通过；`/channels`、`/channels/livechat`、`/channels/telegram`、`/channels/telegram/new` 均返回 HTTP 200。

## 2026-09-17 收件箱搜索区分割线移除（已完成）

- 去掉会话列表中搜索框工具区与下方会话列表之间的横向分割线，使搜索区和会话列表过渡更干净。
- `npm.cmd run build` 通过；源码确认 `.inbox-list-tools` 已无 `border-bottom`；`/inbox?conversation=conv-p-001` 返回 HTTP 200。
- `oxlint src/styles/global.css --max-warnings=0` 不适用于单独 CSS 文件，返回「No files found to lint」。

## 2026-09-17 收件箱 AI 客服导航与筛选解耦（已完成）

- 修复左侧「处理中」下选中 AI 客服时，会话列表 Filter 同步出现 AI Agent 筛选 chip 的问题。
- 左侧 AI 客服导航继续使用 URL 参数 `agent`，只负责当前处理列表的导航过滤与选中态。
- 会话列表 Filter 中的 AI Agent 筛选改用独立 URL 参数 `filterAgent`，与左侧导航互不联动。
- 历史会话切换继续清空会话列表筛选，避免切换会话时残留筛选 chip。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx --max-warnings=0` 通过；`/inbox?status=processing&agent=account-agent&conversation=conv-p-008` 返回 HTTP 200。

## 2026-09-17 收件箱 LiveChat Logo 资产替换（已完成）

- 将收件箱渠道类型中的 LiveChat Logo 替换为用户上传的 `ico.png`，落地为项目资产 `src/assets/livechat-logo.png`。
- 保持现有渠道展示逻辑不变，继续在会话列表与渠道筛选中展示「LiveChat Logo + 渠道名称」。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/styles/global.css src/types.ts src/inboxMockData.ts --max-warnings=0` 通过；`/inbox?status=processing&agent=account-agent&conversation=conv-p-008` 返回 HTTP 200。

## 2026-09-16 收件箱渠道类型 Logo 优化（已完成）

- 会话列表与渠道筛选中的渠道展示从通用线性图标调整为「渠道类型 Logo + 渠道名称」。
- 渠道类型收敛为 `telegram` 与 `livechat` 两类：TG 充值渠道展示 Telegram 纸飞机 Logo，官网／在线客服类渠道展示 LiveChat 气泡 Logo。
- 清理旧的 `web` / `line` 渠道类型和 `IconDesktop` / `IconSend` 旧图标引用，避免渠道类型与界面展示不一致。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts src/styles/global.css --max-warnings=0` 通过；`/inbox?status=all&channel=web-payment&conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱标题字号与历史方向修正（已完成）

- 收件箱栏、会话列表栏、会话详情栏标题字号从 18px 统一调整为 16px，标题栏高度继续保持一致。
- 修正历史会话切换方向：会话按最新在前排序时，顶部「上一个会话」指向更早的历史会话，底部「下一个会话」指向更新的会话。
- 保持无可切换方向时不显示按钮；例如最新会话滚动到底部不显示「下一个会话」，滚动到顶部显示可进入更早历史的「上一个会话」。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/styles/global.css --max-warnings=0` 通过；`/inbox?conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱标题栏与导航细节精修（已完成）

- 会话列表栏标题区拆分为「标题栏」和「工具区」：标题栏仅保留当前状态标题、会话数量和 `Filter`，搜索框与筛选条件标签下移到标题栏下方，避免三栏顶部高度不一致。
- 统一收件箱栏、会话列表栏、会话详情栏标题字号为 18px、标题栏高度为 56px，确保三栏顶部横向对齐。
- 「处理中」展开／收起控件替换为纯 CSS chevron 图标，展开时旋转为向上状态，去除额外按钮感。
- 保持会话详情历史切换规则：只有当前方向存在可切换会话时才显示对应按钮，不展示禁用态。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts --max-warnings=0` 通过；`/inbox?conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱历史切换与多状态消息补强（已完成）

- 保持会话详情中的「上一个会话／下一个会话」只在滚动到顶部或底部时出现；当前方向没有可切换会话时不渲染按钮，避免展示不可操作状态。
- 通用会话 Mock 消息从 5～6 条扩展为 9～10 条，并改为按分钟递增生成时间，避免演示中后续消息时间早于前序消息。
- 按支付查询、账号验证、VIP 权益、AI 已解决、转人工、超时关闭等不同状态生成差异化追问和 AI Agent 回复，让非重点会话也具备完整上下文。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts --max-warnings=0` 通过；`/inbox?conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱会话切换按钮与消息丰富度修正（已完成）

- 修复进入会话后上下会话切换按钮不出现的问题：默认滚动到最新消息时保留底部状态，历史会话存在时展示居中的「下一个会话」按钮。
- 上一个／下一个会话按钮改为在聊天区域水平居中显示，滚动到顶部或底部才出现，离开边缘后自动隐藏。
- 通用会话 Mock 生成逻辑从 3 条扩展为 5～6 条消息，覆盖客户追问、AI Agent 跟进和结束／转人工／超时系统消息，让非重点会话也更接近真实聊天记录。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts --max-warnings=0` 通过；`/inbox?status=all&conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱客户图片消息与左右角色对齐（已完成）

- 会话详情聊天区调整为客户消息在左侧、AI Agent／客服消息在右侧，头像与气泡方向同步更新。
- 使用 `imagegen` 生成一张虚构的客户充值扣款凭证图片，并保存到 `src/assets/mock-payment-receipt.png` 作为项目内演示资产。
- 收件箱 Mock 消息规则调整为客户支持发送文本与图片消息，AI Agent 暂仅支持文本消息；当前 `conv-p-001` 会话包含客户上传扣款截图的演示链路。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts --max-warnings=0` 通过；`/inbox?status=all&conversation=conv-p-001` 返回 HTTP 200。

## 2026-09-16 收件箱三栏工作台与会话气泡优化（已完成）

- 收件箱页面取消独立顶部标题条，将「收件箱」放入左侧收件箱栏顶部；左侧栏、会话列表栏、会话详情栏改为同高平排显示。
- 「收件箱」与「聊天」属于同一栏，通过分割线区分；「聊天」调整为弱一级副标题层级，视觉权重低于「收件箱」。
- 「处理中」改为纯展开／收起分组，不再显示选中态和数量；展开按钮收敛为无背景纯箭头。
- 会话列表卡片移除状态标签，仅保留客户名称、最近消息、渠道、AI Agent 和更新时间。
- 会话详情背景改为纯白；客户消息气泡调整为浅灰色，AI 客服消息气泡调整为浅蓝色，贴近 Text Inbox 的聊天阅读体验。
- 会话详情 Mock 消息扩展为多轮对话，并新增 AI 客服图片消息类型；重点覆盖当前处理会话和历史已解决会话。
- 上一个／下一个会话切换按钮仅在消息区滚动到顶部或底部时出现，默认进入会话不固定展示。
- AI 已解决会话的系统消息从「问题已解决，会话结束」调整为「会话结束」，最终展示为「会话结束 · 时间」。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx src/inboxMockData.ts src/types.ts --max-warnings=0` 通过；`/inbox?status=all&channel=web-payment&conversation=conv-r-002` 返回 HTTP 200。

## 2026-09-16 收件箱工作台体验优化（已完成）

- 收件箱顶部标题区移除描述文本，压缩标题高度并保留通栏分割线，让页面重心回到三栏工作台内容。
- 左侧会话导航标题统一为「聊天」；处理中保持可展开结构，AI Agent 子项增加圆形头像图标与名称，提升识别效率。
- 会话列表标题按状态动态展示：全部为 `All chats`，处理中／已解决／已结束保持中文状态名称；会话列表继续以卡片 hover 和选中态区分当前会话。
- 会话详情顶部简化为仅展示客户名称，移除访客 ID、状态、渠道、AI Agent 和会话 ID 等冗余上下文栏。
- 历史会话切换从详情标题栏移入聊天滚动区：滚动到顶部时展示「上一个会话」，滚动到底部时展示「下一个会话」。
- 消息区补齐客户消息头像与客户名称，系统消息调整为「事件内容 · 时间」同行展示；时间格式统一为当天 `HH:mm`、跨日期 `M月D日 HH:mm`。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx --max-warnings=0` 通过；本地 `/inbox` 返回 HTTP 200，源码断言已覆盖标题、导航、筛选、历史切换与时间格式关键实现。Chrome／Edge headless 截图因本机 GPU 进程不可用未生成截图。

## 2026-09-16 收件箱筛选交互二轮优化（已完成）

- 会话列表移除固定展示的渠道与 AI Agent 筛选，统一收敛到标题右侧 `Filter` 入口；未筛选时仅显示筛选图标与文案，已筛选时按筛选类型显示数量 Badge。
- 点击 `Filter` 后仅展示筛选类型「AI Agent」「Channel」，不在类型菜单中放置重置按钮；选择类型后自动在搜索框下方生成条件标签，并打开该类型的具体选项浮层。
- AI Agent 与 Channel 具体选项均支持搜索、多选与全选；Agent 选项展示头像图标，Channel 选项展示渠道类型图标，选中项整行浅色高亮并在右侧显示勾选。
- 条件标签按选择值动态展示：未选值展示类型名，单选展示具体名称，多选展示 `AI Agent 2`／`Channel 2`；点击标签可重新打开选项浮层，点击 `×` 可移除该筛选条件。
- 筛选选项改为选中即生效，选中或取消选中具体 AI Agent／Channel 后立即刷新列表并写入 URL；不再保留确认按钮和重置按钮，已选条件通过标签 `×` 单项移除。
- 收件箱整体视觉从后台列表进一步调整为客服工作台：会话列表改为圆角会话卡，选中态改为中性灰卡片，客户头像增加稳定色彩区分，会话行内展示状态小标签；右侧会话详情补充底部状态栏，表达 AI 处理中、已解决、已转人工或已关闭。
- `npm.cmd run build` 通过；`oxlint src/pages/InboxPage.tsx --max-warnings=0` 通过；筛选交互已调整为「点筛选图标 → 选筛选类型 → 选具体值后立即生效」，最终 URL 保留多值筛选，Filter Badge 显示筛选类型数量。
- 浏览器补充验证已完成会话卡选中态与详情底部状态栏，截图保存至 `output/playwright/inbox-workbench-polished.png`。

## 2026-09-16 收件箱高保真产品原型（已完成）

- 左侧导航首位新增「收件箱」，接入独立 `/inbox` 路由；保持 AI 客服、知识库与团队原有入口和功能不变。
- 完成「会话状态／会话列表／会话详情」三栏工作台，覆盖处理中、已解决、已结束和全部状态，并支持按 AI 客服展开统计。
- 完成消息内容搜索、渠道与 AI 客服组合筛选、会话详情查看，以及同一访客同一渠道历史会话的上下切换；筛选、选中会话和演示状态均写入 URL。
- 提供 23 条完整 Mock 会话，覆盖处理中、AI 已解决、转人工、15 分钟超时关闭和同一客户多次历史会话；消息区区分客户、AI 客服和系统消息。
- 补齐列表／消息加载、无数据、加载失败与重新加载状态；窄屏下收起状态栏并切换为状态选择器，超窄屏改为上下布局。
- `npm.cmd run build` 通过；Playwright 已验证首位导航、已解决状态、消息内容搜索、历史会话切换、1000px 窄屏布局及既有 AI 客服页面可访问。

## 2026-09-15 产品名称更新（已完成）

- 将产品导航品牌名称与浏览器页面标题统一更新为「IM Support」。

## 2026-08-31 AI 客服预览测试玩家配置（已完成）

- 修复「测试设置」浮层展开时因浮层容器与内容宽度不一致导致的横向溢出和页面抖动；已验证 2048px、1280px、768px 视口下展开与收起均不改变页面宽度和主体位置。
- 在 AI 客服详情页「预览与测试」标题区新增「设置图标＋测试设置」紧凑入口，已配置时显示状态圆点，不额外占用测试区内容空间。
- 点击入口打开轻量浮层，玩家 ID 字段名称与输入框水平并排；不展示冗余说明，主操作统一为「确定并重置对话」。
- 玩家 ID 仅作用于当前预览测试会话，不写入 AI 客服草稿或发布配置；应用或清除玩家 ID 时同步清空当前测试对话。
- 已验证未配置时提示先配置测试玩家，配置 `P10086` 后可模拟查询玩家业务数据；`npm.cmd run build` 通过，浏览器交互与布局符合已确认方案。

## 2026-07-30 IM 平台新增功能与四模块回归测试用例（已完成）

- 通过飞书 CLI 创建《IM平台AI客服新增功能及原有功能回归测试用例》电子表格，包含「测试说明」「测试数据准备」「测试用例」3 个子表。
- 共整理 159 条测试用例，覆盖后台租户创建与系统配置、AI 客服、知识库、团队、人工客服新增字段、WebSocket 消息联动、20 单上限、最少负载随机分配、转人工／转工单生命周期和多租户隔离。
- 原有功能回归严格限定为「客户咨询、客户列表、客服监控、工单列表」；明确排除 AI 回复质量、意图识别、知识检索效果、固定工作流业务正确性和第三方 API 调用。
- 已分 4 个行窗口回读主表，确认 `A1:H160` 共 160 行（1 行表头＋159 条用例）且无截断；表头样式已回读为 `#1664FF` 蓝底、白字、加粗。
- 飞书表格：https://bc53xs53ek.feishu.cn/sheets/NqQ6sK1mdhYZFetngu3cpJJXnqD

## 2026-07-22 当前产品 PRD v1.1 与飞书云文档（已完成）

- 基于当前可见导航、页面组件、统一 reducer 和实际交互，修订 `prd/customer_support_agent_current_prd.md` 为 v1.1；纠正在线红点位置、转人工路由、最近保存、团队成员字段和数据模型等历史偏差。
- PRD 按产品文档粒度覆盖产品范围、客服管理、AI 客服详情、知识库、团队、跨模块状态、异常边界、验收标准与待确认事项；头像仅说明支持的图片类型和当前大小限制，不展开技术实现细节。
- 通过飞书 CLI 以当前用户身份创建云文档，并按 8 个章节顺序写入；云端文档版本更新至 revision 11，全部章节写入无降级警告。
- 已回读目录和关键规则，确认「未命名_副本」、头像类型与大小边界、PDF 50 MB 限制、团队详情不展示优先级、文档解绑后删除等正文实际存在。

## 2026-07-22 AI 客服复制空白页回归修复（已完成）

- 已通过隔离 Edge 会话捕获白屏根因：自定义头像以 data URL 同时写入 AI 客服、客服副本和多个团队成员，触发 localStorage `QuotaExceededError`，未捕获的持久化 effect 异常导致 React 根节点卸载。
- 持久化时移除团队成员中的重复 AI 头像，并将相同 data URL 头像编码为引用；读取时自动恢复真实头像，兼容既有 localStorage 数据。
- localStorage 写入增加异常边界，存储失败不再击穿整个页面；复制后仍停留在客服管理列表，副本保持「原名称_副本」、未发布和关闭接受聊天的既有规则。
- `npm.cmd run build` 与 `oxlint src/store.tsx` 通过；浏览器使用约 3 MB 自定义头像连续复制，副本持久化增量约 3.8 KB，刷新后副本仍存在且未再出现配额异常。

## 2026-07-22 团队详情移除优先级字段（已完成）

- 团队详情成员表格移除「优先级」列及「主要／备份」选择器，只保留成员身份信息与右侧移除操作；名称列自然扩展，不增加替代字段。
- 同步清理仅供优先级选择器使用的组件代码与样式，并更新当前 PRD 和设计基线；底层历史成员数据保持兼容，不影响现有团队成员关系。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；本地服务 `/teams` 返回 HTTP 200。Lint 仅保留既有 Fast Refresh 警告。

## 2026-07-22 团队创建与删除反馈（实现完成，浏览器回归待补）

- 新建团队保存后立即更新列表，并展示居中结果弹窗；弹窗包含成功图标、动态团队名称、已添加成员数量以及「继续创建／完成」操作，关闭弹窗等同于完成。
- 非内置团队删除改为受控危险确认弹窗，明确说明不可恢复、不会删除团队中的客服并展示待删除团队名称；确认后移除团队并反馈「团队已删除」。
- 内置「通用」团队继续隐藏删除入口；被 AI 客服指定转人工引用的团队继续阻止删除并提示先修改配置。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；本机 Playwright 启动 Chromium／Edge 时均因 GPU 进程崩溃退出，页面服务返回 200，真实浏览器交互回归待环境恢复后补充。

## 2026-07-22 知识文档删除场景数据（已完成）

- 新增「文档被绑定删除二次提示.pdf」与「文档未被绑定删除二次提示.pdf」两条处理成功模拟文档，名称直接说明演示场景。
- 已绑定场景关联「客服接待」「退款处理」；未绑定场景不关联任何 AI 客服，可直接演示普通删除二次确认。
- 增加一次性演示数据版本迁移，使已有 `localStorage` 数据自动补齐场景文档，同时避免用户删除未绑定演示文档后刷新页面再次出现。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证两条场景文档、完整 `.pdf` 文件名、绑定阻止删除与未绑定二次确认。

## 2026-07-22 已绑定文档删除提示视觉优化（已完成）

- 无法删除提示中的文档名称补齐文件类型后缀，已包含后缀的文件名不重复追加。
- 被绑定的 AI 客服列表收敛为一个完整浅色信息容器，列表项之间仅使用轻量分隔线，不再逐项使用独立背景色块。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证文件名后缀、绑定客服内容与整体浅色容器视觉。

## 2026-07-22 知识库创建者与删除保护（已完成）

- 知识库列表的创建者统一展示真实用户名称；当前上传用户为 Owen，种子数据使用 Owen、Lana、Mia 等姓名，历史 localStorage 中的组织角色名称会自动迁移为对应用户名称。
- 删除文档前同时检查 AI 客服草稿与已发布配置的绑定关系；已绑定文档禁止删除，弹窗明确提示先解绑，并列出全部绑定该文档的 AI 客服；未绑定文档保留常规二次确认。
- 文档删除不再自动解除 AI 客服绑定关系，reducer 同步增加绑定校验，避免绕过页面交互误删数据。
- 已用页面内受控 Arco Modal 替代知识库删除流程中的全局 `Modal.confirm`，解决 React 19 下点击删除无弹窗的运行时兼容问题。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证创建者姓名、绑定文档阻止删除并列出「客服接待」「退款处理」、未绑定文档显示常规删除确认。

## 2026-07-22 知识文档上传弹窗重构（已完成）

- 知识库与 AI 客服详情的添加文档入口统一使用 Arco Upload 拖拽模式，上方为上传入口，下方为已选文件列表；弹窗标题仅展示「添加文档」，不附加图标。
- 上传规则统一为仅支持 PDF、文件数量不限、单文件最大 50 MB；格式与大小限制合并为一行展示，不合法文件按项拦截且不清空合法文件。
- 上传入口高度收紧为 180px，已选列表保留 PDF 图标、文件名和逐项删除操作，并使用与上传入口一致、覆盖图标、名称与删除区的完整中性浅灰行背景；文件信息和删除按钮采用对称的 16px 左右基准间距，没有合法文件时禁用「添加文档」按钮。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证多选与 accept 属性、限制文案、上传区高度、统一背景色、对称左右间距、选择／删除文件后的按钮状态和弹窗视觉。

## 2026-07-21 团队在线成员字段命名（已完成）

- 团队列表字段「接受聊天」改为「在线成员」，继续以「当前在线成员数／团队成员总数」格式展示。
- 字段语义同时覆盖人工客服和 AI 客服，避免使用「在线人数」造成成员类型歧义。

## 2026-07-21 团队 ID 快捷复制（已完成）

- 团队列表的 ID 值后新增轻量复制图标；默认隐藏，悬浮 ID 单元格或键盘聚焦时显示。
- 点击复制图标仅复制当前行团队 ID，并短暂切换为完成图标反馈复制成功，不触发行跳转或其他操作。

## 2026-07-21 个人资料视觉细节（已完成）

- 详情配置页的大标题统一为 18px；模块标题与字段标签继续维持已确认的 14px 层级。
- 头像调整为 52px，并与「名称」标签及输入框字段组顶部对齐；系统提示词保留内容自适应高度，移除手动拖拽缩放。
- 新建 AI 客服未填写名称时，详情顶部标题显示「未命名」；预览区保留「未命名AI客服」，名称输入框仍保持空值，填写后即时同步真实名称。
- 新建 AI 客服与初始样例中的「记忆」默认开启，仍支持在详情页按需关闭。
- 客服管理卡片的空名称与详情顶部统一显示「未命名」；复制 AI 客服使用「原名称_副本」命名规范，空名称副本使用「未命名_副本」。
- 修复复制带自定义头像的 AI 客服后白屏：持久化时移除团队成员中的重复 AI 头像，并将相同 data URL 头像编码为引用，避免副本反复占用 localStorage；同时捕获存储配额异常，防止持久化失败卸载 React 根节点。
- 已使用约 3 MB 自定义头像数据完成浏览器回归：连续复制后页面保持可用，localStorage 每个副本仅增加约 3.8 KB，刷新后副本仍然存在；`npm.cmd run build` 通过。
- 团队成员选择浮窗的筛选项「成员」统一改为「人工客服」，创建团队与添加成员入口同步生效。
## 2026-07-21 AI 客服详情个人资料重构（已完成）
- 详情导航「AI 客服」调整为「个人资料」，基础资料从新建／编辑弹窗迁移至「人设」区域。
- 人设区新增头像＋名称、简介、团队字段：名称与团队为必填；简介收敛为单行输入框；系统提示词为空时显示单行，输入后自适应至 8 行。
- 人设、记忆、转人工等模块标题统一为 14px 副标题色，字段标签统一为 14px 正文色。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已核验个人资料导航、横向基础资料布局与系统提示词空态。

## 2026-07-21 客服头像在线状态位置（已完成）
- 客服管理卡片的在线红点从头像右下角调整至右上角。

## 2026-07-21 知识文档处理失败闭环（已完成）
- 当前 PRD 补充知识文档「处理失败」状态、失败原因、不可绑定限制，以及「重新处理／删除」操作和验收标准。

## 2026-07-21 PRD 与高保真产品交付分工（已完成）
- 明确高保真产品用于定义页面视觉、布局、控件与可见交互；PRD 用于定义功能、字段、默认值、状态、业务逻辑、校验和验收，不再要求逐页 ASCII。
- PRD 已补充基础信息弹窗显式保存、详情配置自动保存、最近保存时间、重新发布生效、自动转人工轮询和预览测试规则。
- `product-prd-mvp` Skill 与模板已同步并通过格式校验。

## 2026-07-21 正式 PRD 产品语言校正（已完成）
- 当前产品 PRD 移除 Demo、演示、mock、localStorage 和模拟等实现过程表述，改用真实开发 PRD 的产品范围、状态流转和运行时生效规则。
- `product-prd-mvp` Skill 新增约束：产品 Demo 只用于需求澄清与高保真验证，正式 PRD 仅表达产品需求、业务规则和验收标准；已通过格式校验。

## 2026-07-21 当前 PRD 字段级规格与 Skill 校正（已完成）
- 当前产品 PRD 补充客服卡片头像小红点的在线／离线含义，以及 AI 客服详情顶部操作区、记忆和转人工的字段定义、默认值、可见条件和交互规则。
- 强化本地 `product-prd-mvp` Skill：现有产品反向 PRD 必须以可达页面与实现为事实源，并逐页输出页面结构、顶部操作、字段、默认值、状态表达、交互逻辑和校验；已通过格式校验。

## 2026-07-21 当前产品 PRD 反向重建（已完成）
- 新增 `prd/customer_support_agent_current_prd.md`，以当前可达页面与已确认交互为事实源，重建 AI 客服、知识库与团队的正式 PRD 基线。
- 当前信息架构确认仅包含「AI 客服（客服管理、知识库）」与「团队」；隐藏的技能、渠道及历史代码能力不写入当前用户可达范围。
- 文档已涵盖草稿／发布、PDF 文档处理与绑定、通用团队、转人工配置、数据关系、验收标准及待确认的真实线上规则。

## 2026-07-21 系统提示词改为可选（已完成）
- AI 客服详情中的系统提示词移除必填标记与表单校验；发布时不再以该字段为空阻止发布。

## 2026-07-21 隐藏一级导航模块（已完成）
- 侧边栏隐藏「技能」与「渠道」一级入口，保留现有页面与数据，避免影响已配置内容。

## 2026-07-21 本地产品 PRD MVP Skill（已完成）
- 新增项目本地 Skill `product-prd-mvp`，将基于真实用户问题、MVP 边界、信息架构、页面需求、数据规则、异常状态与验收标准的 PRD 写作方法固化为可复用流程。
- 附带可复制的 `prd-template.md`，覆盖文档信息、产品概述、产品架构、功能需求、流程与权限、数据模型、异常边界和验收标准。
- 已通过 Skill 格式校验。

## 2026-07-21 内置通用团队（已完成）
- 新增内置默认团队「通用」，在团队列表中固定置顶，包含全部人工客服与 AI 客服。
- 新建 AI 客服、编辑团队归属以及历史 localStorage 数据迁移时，统一强制保留「通用」团队成员关系。
- 内置团队不提供删除入口；详情页禁用名称、成员增删，避免破坏其全员默认团队属性。
- 通用团队的编辑入口与其他团队一致，收敛至更多菜单；菜单内仅保留「编辑」。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已核验列表置顶与「新建 AI 客服」表单默认选中「通用」。

## 2026-07-21 团队选择器文案统一（已完成）
- AI 客服团队多选下拉中的「全部选择」统一为「全选」。

## 2026-07-21 AI 客服详情知识库文档化（已完成）
- PDF 类型统一使用用户提供的 `file-pdf` 描边 SVG：无底色、与标题同色，并在一级知识库和 AI 客服详情中复用。
- AI 客服详情知识库改为文档列表：保留名称、创建者、创建时间与直接移除图标，移除状态字段和更多菜单；移除操作采用页面受控二次确认，兼容 React 19。
- 「绑定知识库」统一改为「绑定文档」；绑定弹窗仅展示 PDF 图标与文档名称，搜索右侧提供轻量描边「添加文档」入口，复用 PDF 上传与处理流程。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证文档列表、绑定文档弹窗、添加文档弹窗与移除二次确认。

## 2026-07-21 知识库 PDF 图标替换（已完成）
- 知识库文档列表的 PDF 图标替换为 IconPark `file-pdf` 的内联 SVG，保留现有产品主色与 20px 图标尺寸，未新增依赖。
- `npm.cmd run build` 已通过；Playwright 已复验知识库列表中 PDF 图标的真实显示与对齐。

## 2026-07-21 知识库 PDF 图标视觉微调（已完成）
- PDF 图标移除背景底色，颜色调整为与文件标题一致的前景色，并收敛为 18px，避免抢占表格名称字段的视觉层级。
- `npm.cmd run build` 已通过；Playwright 已复验无底色、同色图标与文件名称的对齐效果。


## 2026-07-21 知识库 PDF 文档处理链路（已完成）

- 一级知识库移除「添加 URL」，主操作统一为「添加文档」；上传弹窗仅接收 PDF，并显示「支持的文件类型：pdf」。
- 列表只展示 PDF 文档，名称列收敛为文档图标与文件名；移除大小字段，「最近更新」调整为「创建时间」。
- 状态改为带图标的「处理中／处理成功」；处理中使用 loading 动效，并模拟上传、切片、数据处理到成功的完整过程。
- 更多操作仅保留删除文档，移除查看详情、编辑和更新入口。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证 PDF 列表、添加文档弹窗与上传后自动处理成功流程。

## 2026-07-20 团队成员选择浮窗与转人工边框修正（已完成）

- 创建团队与添加成员统一收敛为「选择成员」字段；点击选择器后在字段下方展开浮窗，浮窗内包含搜索、文字下划线 Tab、全选与「头像＋客服名称＋客服账号」候选列表。
- 转人工选中卡片取消源力蓝描边与浅蓝底，仅以单选圆点表达当前选项；指定团队继续使用「搜索团队」占位文案。
- 团队详情优先级的收起态只显示「主要／备份」，展开菜单逐项展示标题与对应描述文案。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证新建团队选择器浮窗与优先级菜单的标题／描述完整呈现。

## 2026-07-20 优先级选项信息密度微调（已完成）

- 优先级下拉选项的标题与描述取消重复组内间距，描述行高收紧为 18px，保持信息清晰并减少垂直空白。

## 2026-07-20 团队成员选择与转人工选中态优化（已完成）

- 团队详情中的团队名称字段调整为「名称」；成员优先级收起态仅展示「主要／备份」，展开菜单仍保留标题、接待顺序说明与当前选中标记。
- 「添加成员」弹窗的筛选 Tab 改为文字与下划线选中态，移除实色按钮背景；候选客服列表上方新增「全选」，可一次性选择所有可添加客服。
- AI 客服的转人工配置保持「自动」为默认选项，选中方案仅通过单选圆点表达，不使用蓝色描边或浅蓝色卡片背景。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已验证团队详情字段、优先级收起文案、添加成员全选交互及 AI 客服「自动」默认选中状态。

## 2026-07-20 团队创建与优先级展示一致性修正（已完成）

- 新建团队将「团队名称」与输入框收敛为同一字段组，并压缩成员选择模块的组内间距。
- 新建团队的成员选择器与「添加成员」弹窗统一为文字下划线 Tab、搜索、全选和成员列表的同一交互结构。
- 转人工选中方案保持白色背景与中性描边；指定团队选择器占位文案调整为「搜索团队」。
- 客服管理一级页面标题调整为「客服管理」。团队成员优先级收起态仅显示标题；展开菜单明确展示「标题＋描述」。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；Playwright 已确认新建团队包含 Tab 与全选入口，以及优先级下拉包含两项完整的标题与描述。

## 当前阶段

Phase 3：基于源力设计系统实现真实项目方式开发的前端产品演示 Demo。

## 已完成

- 已读取客户支持智能体 V1 PRD。
- 已读取 qiaomu-design、Carbon 组件与模式规范。
- 已生成并验证 qiaomu 四方向视觉预览。
- 已确认采用 Volcengine Copy 的源力设计系统作为项目视觉基线。
- 已完成项目级 `CLAUDE.md` 与 `DESIGN.md`。
- 已完成 React、TypeScript、Vite、Arco Design、Tiptap 项目初始化。
- 已完成源力 Token、TopNav、Sidenav、PageHeader、列表、表格、卡片、Tabs、Drawer 和反馈状态。
- 已完成智能体、流程、技能、知识库、渠道、监控和测试面板的 mock 交互。
- 已完成 localStorage 持久化、危险操作二次确认和核心操作提示。
- 已完成真实富文本编辑器、知识库 URL／文件模拟、监控筛选和命中场景模拟。
- 已将旧项目源码、配置、设计规范和预览产物迁移到当前工作区根目录，保留 `Volcengine Copy` 作为设计参考；未迁移依赖、构建产物和旧 Git 元数据。
- 已补齐当前 TypeScript 版本下的表格回调与事件类型，并修复 Arco Form 的长度校验兼容，保证合法智能体名称可以提交。
- 已完成 Arco Design 迁移收尾，移除未使用的 Ant Design 依赖并同步项目文档口径。
- 已按确认的 ASCII 方案完成首页壳层与智能体列表优化：顶部 Logo 后接侧栏开关、一级导航仅保留智能体／知识库／技能／渠道、内容区使用圆角白色容器，并补充状态筛选和排序。
- 已按第二轮确认方案完成首页视觉重构：TopNav 与 Sidenav 使用同一导航背景，右侧内容区采用白底、描边和圆角；智能体卡片改为桌面三列，并实现悬浮时仅显示「配置、更多」。
- 已将智能体卡片更多菜单收敛为「复制智能体、删除智能体」，删除使用受控 Arco Modal 二次确认；同时补齐 Button ref 转发，修复 Arco Dropdown 在 React 19 下定位到页面左上角的问题。
- 已按最终确认的首页 ASCII 重构视觉：TopNav 与 Sidenav 共用无分割线的浅蓝导航壳层，右侧内容区与侧栏并排；智能体列表仅保留名称搜索与发布状态筛选。
- 智能体卡片已改为固定窄比例、按可用宽度自动换行，仅展示头像、名称、发布状态、简介和最近更新时间；已移除流程、知识库与排序字段，悬浮操作中的更多按钮改为纯图标。
- 已修正截图视觉对齐：侧边栏选中态使用浅灰蓝底与深灰文字，智能体名称使用 16px 深色模块标题；覆盖通用相邻卡片间距，保证同一行卡片等高对齐。

## 进行中

- 保留后续根据实际评审反馈进行的视觉和交互微调。

## 待办

- 根据 PRD 评审继续补充或调整细节；当前无已知功能阻塞。

## 阻塞

- 暂无。

## 最近验证

- 源力设计系统包含 Token、7 类 UI Kit 和 32 个组件。
- 源力系统存在 README 中 48px TopNav / 200px Sidenav 与 Token 中 56px / 240px 的尺寸冲突；项目统一采用 48px / 200px，以匹配其组件 CSS 和页面骨架。
- `npm run build`：通过；Vite 产物生成成功；仅有大包体积提示。
- `npm run lint`：通过；仅有 `src/store.tsx` 与 `src/components/ui.tsx` 的 Fast Refresh 提示。
- `npm install`：已移除未使用的 `antd` 与 `@ant-design/icons`，`npm ls antd @ant-design/icons --depth=0` 为空。
- Playwright：已验证迁移后新建智能体保存、详情页跳转和流程页签路由切换。
- Playwright：首页已验证导航层级、侧边栏选中态、侧栏收起／展开和内容区自适应；实际截图与确认的 ASCII 结构一致。
- Playwright：已验证首页默认态、卡片悬浮态、更多菜单、删除二次确认和侧栏收起／展开；最终截图位于 `output/playwright/home-final.png`，菜单截图位于 `output/playwright/home-more-menu-fixed.png`。
- Playwright：首页最终视觉已验证默认态、卡片悬浮态和纯图标更多菜单；截图位于 `output/playwright/home-redesign-default.png` 与 `output/playwright/home-redesign-menu.png`。
- Playwright：首页已复验侧边栏选中／未选中状态、智能体标题层级与卡片等高对齐；截图位于 `output/playwright/home-title-aligned.png`。
- 浏览器控制台：核心交互可用；Arco 在 React 19 下仍产生 `element.ref` 兼容性日志，未阻断当前页面操作。

## 新增项目原则

- 这是按真实 B 端 SaaS 项目方式开发的前端演示 Demo，不是带解释性文案的静态产品原型。
- 只模拟后端数据和结果，不模拟产品说明书；界面文案只服务于任务、状态、操作、校验和反馈。
- 后续按模块逐一对齐，先解决信息架构、核心任务、状态与交互，再做视觉优化。
- Arco Design 继续作为组件基础，qiaomu-design 负责设计审查和视觉交互指导，PRD 负责功能边界。

## 2026-07-10 首页壳层微调（已验证）

- TopNav 与 Sidenav 的背景色统一为 `#F4F6FF`。
- 收起侧边栏后，内容区左侧保留 12px 壳层间距，避免选中菜单与内容容器紧贴。
- 白色内容容器移除底部浅描边，保留上方与两侧描边和圆角。
- 已通过 `npm run build`、`npm run lint` 与 Playwright 展开／收起状态复验；截图：`output/playwright/home-shell-expanded-final.png`、`output/playwright/home-shell-collapsed-stable.png`。

## 2026-07-10 首页导航色与收起侧边栏微调

- 已将 TopNav 与 Sidenav 的设计系统背景色更新为 `#F9FAFD`。
- 已移除收起侧边栏右侧的边框与投影样式。
- `npm run build`、`npm run lint` 已通过；本轮浏览器截图复验因本地浏览器工具额度限制待补。

## 2026-07-10 一级模块统一设计基线（已固化）

- 已将首页确认的 TopNav、Sidenav、内容容器、PageHeader 通栏分隔线、列表结构、统一状态交互和 ASCII 确认门禁固化到 `CLAUDE.md` 与 `DESIGN.md`。
- 后续智能体、知识库、技能、渠道一级模块复用同一套壳层、Token、Arco 组件行为和页面节奏；业务字段仍以各自 PRD 为准。

## 2026-07-10 智能体头像字段（已完成）

- 新建／编辑智能体表单底部新增头像字段，默认使用项目占位头像；点击头像直接选择图片，不增加额外上传按钮或常驻说明。
- 智能体卡片改为读取智能体头像字段；头像数据随 mock 状态和 localStorage 持久化。
- 已生成并接入占位头像：`public/assets/agent-avatar-placeholder.png`。
- `npm run build`、`npm run lint` 已通过；仅保留既有 Fast Refresh 与构建体积提示。

## 2026-07-10 智能体详情页框架与智能体配置（已完成）

- 已按确认的 ASCII 完成详情页四层框架：顶部操作栏、左侧配置／监控导航、中间配置区、右侧浅蓝预览与测试面板。
- 顶部智能体名称支持点击打开「编辑智能体」弹窗，并展示发布状态与最近保存时间。
- 已完成「智能体」配置界面：基础信息、人设、记忆、异常回复；记忆支持开关、自定义／永久有效切换、1—365 天滑杆和 1—30 历史会话数滑杆。
- 已移除详情页「保存配置」按钮，配置项修改采用 400ms 防抖自动保存；发布仍保持独立操作。
- 已补充 Arco Slider 封装和详情页设计规则至 `DESIGN.md`。
- `npm run build`、`npm.cmd run lint` 已通过；仅保留既有 Fast Refresh 警告与 Vite 构建体积提示。
- 本轮未重做流程、技能、知识库、防护措施、环境变量和监控内容，待后续逐模块对齐。

## 2026-07-10 智能体详情页框架修正（已完成）

- 详情页路由已改为独立全屏页面，不再复用首页 TopNav 和一级 Sidenav。
- 已移除详情页顶部「测试」按钮；右侧固定预览面板保留轻量对话测试，不展示旧测试 Drawer 的命中调试信息。
- 详情页输入框、文本域、选择器和日期控件统一改为白底细描边，聚焦态使用源力蓝描边。
- 中间配置区与右侧预览面板之间新增可拖拽分隔线，可自由调整右侧面板宽度并限制最小／最大宽度。
- 基础信息继续只通过顶部智能体名称打开「编辑智能体」弹窗维护，不在智能体配置页面展示。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；lint 仅保留项目既有 Fast Refresh 警告，Vite 仍提示构建包体积较大。

## 2026-07-10 智能体配置最终结构修正（已完成）

- 顶部返回图标已移动到智能体名称左侧，移除详情页面包屑和发布按钮左侧的返回按钮。
- 已从「智能体」配置页面移除基础信息模块，基础信息继续通过顶部名称打开编辑弹窗维护。
- 记忆关闭时收起有效期、有效天数和历史会话数；开启后恢复原配置。
- 「永久有效／自定义」改为浅灰色分段容器，选中项使用白色背景和源力蓝文字。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；仅保留项目既有 Fast Refresh 警告和构建体积提示。

## 2026-07-10 记忆配置层级与预览面板高度修正（已完成）

- 已将「记忆有效期」与「历史会话数」调整为记忆模块下的两个同等级配置块，有效天数仅作为记忆有效期的下级配置。
- 已补齐右侧预览与测试面板的全高布局，面板占满详情页工作区，输入框固定在底部。
- `npm.cmd run build`、`npm.cmd run lint` 已通过；仅保留项目既有 Fast Refresh 警告和构建体积提示。

## 2026-07-10 视觉参考转译工作流（已固化）

- 已将参考截图的使用范围扩展并固化为：布局、视觉样式、颜色关系、色系、字体层级、控件状态、圆角、描边、阴影、信息密度和整体风格。
- 已明确执行顺序：PRD 定义内容与功能，截图提炼布局和视觉语言，源力设计系统完成 Token 转译，Arco Design 完成组件实现，最后按四层标准验收。
- 后续模块默认先输出 ASCII、功能说明、交互说明、字段层级、状态和校验说明；批量开发必须获得用户明确授权。

## 2026-07-11 一级模块与详情配置批量重构（已完成首轮）

- 知识库一级模块已改为资源操作区、搜索工具栏和知识文档列表，支持添加 URL、批量添加文件、状态展示、详情查看和更多菜单删除；字段与校验严格对应 PRD。
- 技能一级模块已改为固定图标卡片、启用状态、描述、更新时间和更多菜单，上传仅支持 `.zip`／`.md`，移除常驻关系解释文案。
- 渠道一级模块已改为渠道类型页签、搜索、表格和分步骤 LiveChat 配置弹窗，未新增 PRD 外渠道字段。
- 智能体详情页新增统一的当前配置标题；流程、技能、知识库、防护措施、环境变量和监控已替换为统一详情页布局，技能和知识库继续只从已启用／已上传成功资源弹窗选择。
- 详情页监控已拆分为「数据概览」和「会话记录」两个菜单页面，保留 PRD 规定筛选项、指标、会话字段和详情抽屉。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，并用 Playwright 验证 `/knowledge`、`/skills`、`/channels`、流程详情和技能选择弹窗；浏览器仅保留项目既有 React 19 与 Arco 的 `element.ref` 兼容性日志。

## 2026-07-11 一级模块视觉基线对齐（已完成）

- 知识库、技能、渠道内容区标题已统一复用智能体首页的标题字号和垂直对齐方式。
- 三个一级模块的搜索框宽度已统一为智能体首页搜索框宽度 `244px`，并通过 Playwright 截图复核。
- 未修改智能体首页及其已确认结构。
- 进一步将三个一级模块的标题栏通栏结构、内外边距和底部分割线完全对齐智能体首页。
- 已核对标题栏运行态高度与智能体首页一致；技能、渠道标题栏下方重复的工具栏上边线已移除，保留单条分割线。
- 通过 DOM 实测修正知识库标题栏高度差异（`52.57px` → `60.57px`，与智能体一致），并将渠道类型页签从 `36px` 收敛为 `32px`，避免渠道标题栏被撑高。
- 渠道页已统一为「标题栏：渠道 + 新建渠道」；「LiveChat」单独位于下一行，搜索框再位于其下方；LiveChat 图标使用用户提供的 Logo Mark，并继续使用项目设计系统布局和间距。

## 2026-07-11 首页卡片与导航细节修正（已完成）

- 一级侧边栏图标统一为 `16x16`。
- 内容白色容器恢复底部描边。
- 智能体与技能卡片改为自适应整数列，卡片宽度随内容区宽度分配，避免横向残余空间无法容纳下一张卡片。
- 技能卡片改为头像、名称、启用开关同排；描述最多显示两行并省略；更新时间与更多菜单同排。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，并用 Playwright 截图复核首页与技能页。

## 2026-07-11 技能卡片与智能体卡片统一（已完成）

- 技能卡片已复用智能体卡片的头像尺寸、标题层级、颜色、卡片高度、圆角、描边和悬浮态。
- 已移除技能卡片「最近更新时间」字段。
- 描述限制为最多两行，超出省略；启用开关与头像、名称同排。
- 更多菜单改为仅显示 `···` 图标，默认隐藏，悬浮卡片时显示，菜单仅提供删除技能。
- 智能体与技能继续使用 `auto-fit` 自适应整数列，尽量在当前宽度内展示更多卡片。
- 已通过 `npm.cmd run build`、`npm.cmd run lint` 和 Playwright 悬浮交互验证。

## 2026-07-11 卡片宽度与技能描述自检修正（已完成）

- 技能卡片描述改为直接复用智能体卡片的 `agent-description` Token，字号、颜色、行高和两行省略规则保持一致。
- 技能模拟描述补充为可换行的多行文案，统一限制为最多两行。
- 智能体与技能卡片桌面宽度收敛到 320–338px，使用整数卡片列并保留最小宽度，避免 `1fr` 将卡片过度拉宽。
- 已通过构建、Lint 和 Playwright 实测：两类卡片宽度均为 338px，描述高度为 40px、字号为 12px、行数限制为 2，技能更多按钮悬浮时覆盖显示。

## 2026-07-11 技能卡片紧凑布局修正（已完成）

- 技能卡片高度从 156px 收敛到 132px，保留头像、名称、开关和两行描述的可读空间。
- 更多按钮悬浮层移动到描述文案区域上方，覆盖当前卡片内容显示，不再占用独立底部行。
- 模拟技能描述补充为超过两行的完整业务文案，由 CSS 统一截断为两行。
- Playwright 实测卡片高度 132px，描述可视高度 40px、实际内容高度 60px，更多按钮悬浮层定位在描述区域内。

## 2026-07-11 卡片均分与悬浮态统一修正（已完成）

- 智能体状态筛选文案由「全部发布状态」改为「全部状态」。
- 智能体与技能卡片改为以 320px 最小宽度计算可容纳列数，再将当前列等宽均分，固定列间距并减少右侧碎片空间。
- 技能卡片更多按钮复用智能体卡片的悬浮覆盖逻辑，补充浅色背景和描边，悬浮时覆盖描述区域显示。
- 技能卡片头像、标题、描述的上下间距统一为同一套卡片内边距和描述 Token。
- 已通过构建、Playwright 实测：技能卡片宽度 383px、三列等宽，描述起始位置一致，更多按钮背景为设计系统浅色填充；服务返回 HTTP 200。

## 2026-07-11 卡片列数计算与悬浮内容切换修正（已完成）

- 卡片网格改用 `auto-fill`，保留可容纳的空列，以 320px 最小宽度和固定间距计算最大列数，避免剩余空间还能放下同宽卡片时却将已有卡片拉宽。
- 技能卡片悬浮时隐藏描述文案，显示底部更多操作层，按钮尺寸、背景和定位与智能体卡片一致。
- 已通过 `npm.cmd run build`、`npm.cmd run lint` 和 Playwright 验证：技能悬浮时描述透明度为 0、操作层透明度为 1；智能体当前两张卡片均为 383px，技能当前三张卡片均为 383px。

## 2026-07-11 技能上传弹窗（已完成）

- 技能模块的「上传技能」改为打开上传弹窗，弹窗由标题、拖拽／点击上传区域和底部取消／确认操作栏组成。
- 支持不限大小的 `.zip`、`.skill`、`.md` 文件；`SKILL.md` 文本校验名称与描述字段，压缩包和 `.skill` 文件保留根目录 `SKILL.md` 的模拟校验入口。
- 已实现未选择文件禁用确认、文件名展示、上传中状态、格式错误提示和模拟上传后写入技能列表。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，服务返回 HTTP 200；浏览器自动化复核因本机 Playwright CLI 依赖缓存权限错误未执行。

## 2026-07-11 技能卡片底部留白修正（已完成）

- 移除技能卡片底部内边距和描述底部外边距，取消固定高度，改为由统一的两行描述内容驱动卡片高度。
- 保留悬浮更多按钮的绝对定位，不改变卡片字段和悬浮交互。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，服务返回 HTTP 200。

## 2026-07-11 技能卡片底部间距恢复（已完成）

- 恢复技能卡片底部 16px 内边距，修正底部间距为 0 导致的卡片贴底问题。
- 保持内容高度驱动，不恢复之前的固定高度和额外空白。
## 2026-07-11 流程详情页结构重构（已完成）

- 流程页移除详情配置区外层模块大标题，改为左侧“流程”标题、新增流程按钮、搜索框与流程列表，右侧保留当前流程配置区。
- 流程名称、触发器与内容编辑模块使用分割线区分，继续沿用自动保存，不增加保存按钮。
- 富文本工具栏补充 H1、H2、H3 与有序列表操作，支持流程内容的标题层级与排序表达。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，并验证流程详情 URL 返回 HTTP 200。
## 2026-07-11 流程页细节对齐（已完成）

- 新增流程改为仅显示加号图标，保留 aria-label，避免左侧操作区出现额外文案。
- 流程配置区移除“流程配置”字段，保留当前流程名称与自动保存状态。
- 配置模块之间的分割线扩展为通栏，覆盖右侧配置区可用宽度。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，流程详情 URL 返回 HTTP 200。
## 2026-07-11 流程页编辑器与模块分隔修正（已完成）

- 左侧流程标题栏与搜索、流程列表拆为两个独立模块，并增加通栏分割线。
- 右侧配置区移除自动保存字段，保留流程名称、触发器和内容配置。
- 内容编辑器仅保留 H1、H2、H3、有序列表、撤销和重做。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，流程详情 URL 返回 HTTP 200。
## 2026-07-11 流程配置区通栏分割线修正（已完成）

- 移除右侧流程名称顶部大标题，流程名称仅保留为表单字段。
- 调整配置区布局层级，使流程名称、触发器和内容之间的分割线覆盖整个右侧配置区。
- 左侧流程标题栏与流程列表模块继续保持独立分隔。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，并重新启动服务验证流程详情 URL 返回 HTTP 200。
## 2026-07-11 智能体技能与知识库配置页重构（已完成）

- 技能配置页改为标题、添加按钮、搜索框与技能卡片，卡片展示头像、名称、描述和分离操作。
- 知识库配置页改为与一级知识库一致的名称、创建者、最近更新、状态字段，操作栏改为查看详情和分离智能体。
- 添加技能弹窗改为搜索加资源行，展示头像、名称和描述，点击整行立即添加。
- 添加知识库弹窗参考一级知识库列表结构做紧凑适配，隐藏创建者、最近更新时间和状态，点击整行立即添加。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，技能和知识库详情 URL 均返回 HTTP 200。

## 2026-07-11 智能体详情页标题、资源列表与控件状态统一（已完成）

- 除流程外，带主操作的详情模块统一为“页面标题＋操作按钮”同排结构；移除技能、知识库、防护措施和环境变量的重复标题及标题下说明文案。
- 技能配置卡片复用一级技能模块的头像、标题、描述、尺寸、圆角、描边和悬浮交互；移除启用开关，悬浮更多菜单仅提供“分离智能体”。
- 知识库配置列表继续复用一级知识库字段结构，操作栏仅保留“分离智能体”；移除 Table 外重复空状态，搜索无结果和未绑定状态均只展示一个空状态。
- 技能与知识库配置页搜索框已接入实际列表筛选；添加弹窗继续保持点击资源行立即添加。
- 修正详情页 Arco 输入框内部原生控件与外层组件重复聚焦描边的问题，保留单一外层蓝色聚焦状态。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；技能、知识库、防护措施和环境变量详情 URL 均返回 HTTP 200。Playwright CLI 复核因本机 npm 缓存权限错误未执行。

## 2026-07-11 全项目表单控件聚焦状态修复（已完成）

- 完成 Input、Search、Textarea、NumberInput、Select、Picker 与 Slider 内部输入控件的聚焦样式检查。
- 移除原生表单元素与 Arco 外层组件重复叠加的 `focus-visible` 轮廓，统一由组件外层显示单一状态描边。
- 保留按钮、链接和其他可操作元素的键盘焦点提示，不降低键盘导航可见性。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；仅保留项目既有 Fast Refresh 与构建包体积提示。

## 2026-07-11 详情页组件结构与流程编辑区复核修正（已完成）

- Search、NumberInput 等复合输入控件改为由 Arco 外层容器统一绘制背景、描边和聚焦状态，嵌套原生输入不再重复绘制边框。
- 智能体详情页技能卡片改为直接使用与一级技能模块相同的 Arco Card 组件、DOM 结构、标题、头像、两行描述、描边和悬浮更多菜单；仅按配置场景移除启用开关并将菜单操作替换为“分离智能体”。
- 流程内容编辑器改为完整占用右侧配置区剩余宽度，并根据浏览器可视高度铺满剩余页面空间。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，详情页返回 HTTP 200；使用 Edge 无界面截图复核技能卡片和流程编辑区布局。

## 2026-07-11 详情页条件交互与资源操作修正（已完成）

- 防护措施的“违规时操作”改为条件表单：选择“结束会话”时隐藏并清空话术；选择“回复固定话术”时显示“回复话术”输入框。
- 记忆配置移除滑杆上方重复的数值和“有效天数”副标题，保留“记忆有效期／历史会话数”同级标题、滑杆与数值输入框。
- 添加技能与添加知识库弹窗统一补足搜索框和资源列表间距；添加知识库移除表头及类型文字列，仅展示类型图标、名称与大小或 URL。
- 智能体详情页知识库操作改为方形更多图标，菜单仅提供“分离智能体”。
- 一级智能体卡片整卡支持鼠标点击及键盘 Enter／Space 进入详情，卡片内部配置和更多操作保持独立，避免冒泡触发跳转。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；智能体首页及智能体、技能、知识库、防护措施详情地址均返回 HTTP 200。浏览器自动化 CLI 因本机未安装且 npm 缓存权限异常未能执行。

## 2026-07-11 LiveChat 渠道配置向导重构（已完成）

- 将 LiveChat 渠道配置由 5 步收敛为“基础信息、访问凭证、服务配置”3 步，移除配置区重复标题和 Modal 默认操作栏。
- 访问凭证步骤点击“下一步”后自动校验凭证并模拟拉取 Group，校验期间显示 loading，成功后直接进入服务配置，不增加结果说明文案。
- 服务配置按“Group 配置、接待配置、转人工”拆为默认收起的独立折叠模块，支持同时展开多个模块；渠道状态保留为常驻配置项。
- 接待 Group 与转人工 Group 改为多选下拉列表，选项包含复选框、名称首字头像和 Group 名称，已选值使用紧凑标签并在溢出时显示数量。
- 热门问题改为单行输入列表，支持编辑、逐条删除，并可在空白输入框中按 Enter 新增一条。
- 转人工开关与字段同排；关闭后隐藏转人工 Group 和转人工失败话术，保存时清空对应绑定值。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，渠道页面 `http://127.0.0.1:4175/channels` 返回 HTTP 200；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 知识库添加入口与全局列表操作统一（已完成）

- 一级知识库将“添加 URL、添加文件”从内容区操作卡移动到标题栏，统一为次按钮和主按钮。
- 添加文件改为独立弹窗，包含标题、拖拽／点击上传区、已选文件反馈、支持格式标签和底部“添加文件”操作；继续沿用既有单文件 100 MB 与格式校验规则。
- 添加 URL 改为独立紧凑弹窗，保留 PRD 要求的 URL 必填项与名称选填项，不引入网站地图、整站抓取、自动刷新等范围外功能。
- 审计当前实际路由中的所有列表操作栏；一级知识库和智能体详情知识库原有更多菜单继续保留，渠道、防护措施、环境变量、会话记录统一改为 32×32 方形更多按钮。
- 所有删除／分离类菜单项继续使用危险色，并在执行前显示确认弹窗；更多按钮补充了包含资源名称的 aria-label。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；知识库、渠道、防护措施、环境变量和会话记录页面均返回 HTTP 200，仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 知识库列表文案降噪（已完成）

- 移除知识库搜索栏右侧重复的支持格式常驻说明，仅保留搜索控件。
- 文件格式信息继续在“添加文件”弹窗内通过格式标签呈现，避免列表页重复解释。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，知识库页面 `http://127.0.0.1:4175/knowledge` 返回 HTTP 200；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 添加 URL 表单 PRD 字段修正（已完成）

- 按 PRD 第 15.4 节恢复“名称”选填字段；填写后作为知识文档名称，留空时使用 URL 作为默认名称。
- URL 继续保持必填，并按 PRD 校验 `https://` 前缀及展示示例错误提示。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 防护措施启用状态交互修正（已完成）

- 智能体详情页防护措施列表将只读状态标签改为 Arco Design 开关，直接表达并控制启用状态。
- 开关切换后同步写入智能体草稿配置并更新最近更新时间；补充包含防护措施名称的无障碍标签。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，防护措施页面 `http://127.0.0.1:4175/agents/agent-support/guards` 返回 HTTP 200；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 全项目场景化 Mock 数据重建（已完成）

- 按 PRD 对象关系重建场景数据：6 个智能体、12 个知识文档、8 个技能、4 个 LiveChat 渠道和 24 条会话记录。
- 智能体覆盖 4 个已发布与 2 个草稿状态；“客服接待”明确保留草稿配置与已发布配置差异，用于验证测试面板和线上渠道的配置隔离。
- 流程覆盖订单、退款、售后、转人工、售前线索、物流异常和会员权益，内容使用标题与有序步骤结构。
- 知识文档覆盖 URL、PDF、TXT、MD、DOC、DOCX、XLSX、CSV，以及成功、添加中、失败状态；技能和渠道同时覆盖启用与关闭状态。
- 防护措施覆盖结束会话、回复固定话术、启用和关闭；环境变量使用明显的沙箱地址和模拟配置，不包含真实凭证。
- 会话覆盖进行中、已结束、已转人工、异常，并包含流程、知识、技能、防护措施和模型异常等真实命中链路。
- 本地存储升级为独立的 `lingxi-agent-prototype:v2:scenario-20260711`，保留旧 `v1` 数据且不做删除；重启开发服务避免热更新将旧运行态写入新场景版本。
- 已通过 TypeScript 构建、Lint、跨对象引用校验和状态分布校验；13 个一级模块及详情路由均返回 HTTP 200，仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-11 渠道启用状态交互修正（已完成）

- 一级「渠道」列表的启用状态由只读标签改为 Arco Design 开关，启用与关闭状态可直接切换。
- 切换后同步写入渠道 Mock 状态、刷新最近更新时间，并提供「渠道已启用／已关闭」反馈。
- 已通过 `npm.cmd run build` 与 `npm.cmd run lint`；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-14 智能体详情绑定与流程字段文案修正（已完成）

- 智能体详情页技能、知识库模块的主操作按钮分别统一为「绑定技能」「绑定知识库」，准确表达与当前智能体建立关联的业务动作。
- 流程配置字段「触发器」调整为「触发条件」，与流程触发规则的业务含义一致。
- 已通过 `npm.cmd run build` 与 `npm.cmd run lint`；仅保留项目既有 Fast Refresh 和构建包体积提示。

## 2026-07-20 AI 客服与团队协作模型（已完成）

- 产品定位调整为聊天平台中的 AI 客服：侧边栏以「AI客服」为一级分组，包含「客服管理」与「知识库」；新增一级「团队」，保留技能与渠道入口。
- 客服管理、新建与详情页统一采用「AI客服」业务文案；新建／编辑 AI客服可在简介下方绑定多个团队。
- AI客服详情仅保留「AI客服」和「知识库」配置；旧详情子路由自动回到 AI客服配置。新增默认开启的转人工配置，支持自动转同团队在线成员或指定团队，并在发布时校验团队依赖。
- AI客服卡片头像改为圆形，接受聊天时显示红点；详情页顶部提供「接受聊天」开关，未发布时禁止开启。发布状态保留文字，不再使用状态圆点。
- 新增团队列表、创建团队、团队详情与成员维护：支持全选／人工成员／AI客服筛选，显示团队 ID、接收聊天人数、成员在线状态和主要／备份优先级；编辑在底部保存前仅保留草稿状态。
- 团队删除会校验是否被指定转人工配置引用，避免产生无效路由目标。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`，并验证 `http://127.0.0.1:4175/agents`、`/teams`、`/agents/agent-support/agent` 均返回 HTTP 200；构建仅保留项目既有包体积与 Fast Refresh 警告。

## 2026-07-20 团队界面与团队选择器统一（已完成）

- 团队一级页面接入统一的标题栏、搜索工具栏、列表和内容容器样式；团队详情沿用同一工作区宽度与页面节奏。
- 团队 ID 统一为纯数字格式，并为旧本地存储中的 `team-support` 等标识增加无损映射，保证 AI 客服绑定和指定团队转人工配置继续有效。
- 团队头像统一为方形文字头像，并按团队标识生成稳定的彩色背景；AI 客服二级菜单「知识库」移除前置图标。
- 新建／编辑 AI 客服和指定团队转人工复用统一团队选择器，支持彩色头像标签、逐项移除、清空、搜索、全部选择与选中标记；指定团队选择器仅在对应方案选中时出现在卡片内部。
- AI 客服详情页顶部「接受聊天」与「发布」操作间距已拉开。
- `npm.cmd run build`、`npm.cmd run lint` 通过；仅保留既有 Vite 包体积提示与 Fast Refresh 警告。浏览器回归因当前沙箱启动后台 Vite 时 Windows 环境变量存在 `Path`／`PATH` 重复而未完成，未将该项记为已验证。

## 2026-07-20 团队页面与知识库标题视觉修正（已完成）

- AI 客服详情「知识库」移除重复页内标题，只保留与「绑定知识库」并排的一组标题操作栏。
- 团队一级页改为与客服管理、知识库一致的 `PageHeader → FilterBar → Table` 结构；名称列统一为方形团队头像、团队名称与「N 名成员」两行信息。
- 新建团队弹窗重构为紧凑表单：团队名称、成员搜索、成员／AI客服筛选与可滚动成员列表；成员行仅保留头像、客服名称与客服账号。
- 团队详情移除「编辑团队」标题，采用返回栏加居中编辑区；成员管理收敛为成员数、添加成员、搜索、优先级与移除操作，复用项目现有 Token 和 Arco 表格节奏。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`。Playwright 已实际验证团队列表、新建团队弹窗、团队详情和 AI 客服知识库页面；仅保留既有 React 19 `element.ref` 兼容日志及 Vite 包体积提示。

## 2026-07-20 团队详情操作层级与优先级选择器打磨（已完成）

- 团队详情将「添加成员」移至成员数量标题同行，成员搜索框独立置于下一行。
- 优先级控件重构为两行信息选择器：主标题表达「主要／备份」，副标题说明接待顺序；下拉选项保留当前选中标记。
- 修正团队列表名称列的原生按钮默认样式，移除错误的描边与背景色块，仅保留团队头像和两行文字信息。
- 已通过 `npm.cmd run build`、`npm.cmd run lint`；Playwright 已复核团队详情优先级下拉和团队列表名称单元格。

## 2026-07-21 知识文档失败状态与 AI 客服状态样本（已完成）

- 知识库默认数据新增一条 PDF 文档处理失败样本，失败行展示明确失败原因；失败文档不会进入可绑定资源范围。
- 失败文档的更多操作菜单提供「重新处理」；触发后依次呈现上传、切片、处理中和处理成功状态。
- AI 客服卡片保留并实际展示三组样本：已发布在线（头像红点）、已发布离线、未发布离线。
- 已通过 `npm.cmd run build`、`npm.cmd run lint` 和 Playwright 实际页面核验；lint 仅保留既有 Fast Refresh 警告，构建仅保留包体积提示。

## 2026-07-22 客服与团队头像实体规范统一（已完成）

- AI 客服详情「个人资料」头像改为圆形，并在右下角增加轻量编辑图标；整枚头像继续作为唯一上传入口，保留 Tooltip 与无障碍名称。
- 详情右侧「预览与测试」空态头像同步为圆形；客服管理卡片与团队成员中的客服头像继续保持圆形。
- 团队头像仍由独立 `TeamAvatar` 组件使用方形彩色文字头像，未受客服头像样式影响。
- 已通过 `npm.cmd run build` 与 `npm.cmd run lint`；Playwright 已实际核验 AI 客服详情和团队列表。仅保留项目既有 React 19 `element.ref` 开发期日志、Fast Refresh 警告与 Vite 包体积提示。
