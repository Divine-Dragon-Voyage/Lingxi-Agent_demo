# 灵犀智能体前端演示项目规范

## 项目目标

实现客户支持智能体 V1 的可运行前端产品演示 Demo。严格以 `customer_support_agent_v1_prd.md` 为功能边界，使用 mock service 和 localStorage 模拟后端状态。它按真实项目方式开发，只是不接真实后端。

## 技术约束

- 工作流模块按 `prd/workflow_demo_prd.md` 的已确认增量需求开发，覆盖资源中心画布编排与 AI Agent 绑定／解绑；画布使用 React Flow，业务执行仅使用模拟数据。

- React + TypeScript + Vite + Arco Design。
- 富文本编辑器使用 Tiptap，不用普通 textarea 伪装。
- 不接真实后端，不增加登录、权限、计费或 PRD 外渠道。
- 业务状态通过统一 reducer 管理，禁止页面之间各自维护同一份实体状态。
- 所有颜色、间距、圆角、阴影和字号优先使用 `src/styles/tokens.css`。

## 设计约束

- `DESIGN.md` 是项目设计系统唯一事实源。
- 设计基线来自 Volcengine Copy 的源力设计系统，使用 React/Arco Design 做工程化转译。
- PRD 决定功能、页面结构和交互范围；源力设计系统决定视觉 Token；参考截图只用于布局关系、信息密度、导航结构、内容容器、选中态和页面节奏。
- 参考截图不得带入竞品文案、品牌、业务模块或组件样式；最终页面必须统一使用项目 Token 和 Arco Design，确保全项目属于同一产品。
- 页面保持 TopNav + Sidenav + PageHeader + Content 的 B 端骨架。
- 顶部导航栏与侧边栏使用同一背景色；顶部左侧顺序固定为产品 Logo 后接侧边栏收起／展开按钮，右侧仅保留个人中心头像。
- 一级侧边栏包含 AI 客服、团队、技能和渠道；AI 客服下包含「客服管理」「知识库」二级菜单，二级菜单不显示图标。监控只属于 AI 客服详情内的功能，不作为一级模块。
- 内容区使用带圆角的白色容器，与页面外层背景形成层级。
- 颜色使用源力蓝 `#1664FF` 作为唯一品牌主色，状态色语义固定。
- 间距使用 4px 栅格，默认控件高度 32px，导航高度 48px。
- 中文使用系统字体栈，正文不小于 14px；数字使用等宽或 tabular numerals。
- 界面文案必须服务于真实任务：只保留页面标题、字段标签、状态、操作、校验、反馈和必要的业务说明。
- 禁止把实现逻辑、数据关系、演示目的或组件行为写成无关紧要的提示文案，例如“关闭或删除技能会同步解除智能体绑定关系”等；这类内容只有在 PRD 明确要求或确实影响当前决策时才出现。
- Demo 只模拟后端结果，不模拟产品说明书；界面中的新增、编辑、删除、发布、绑定、分离、上传、筛选、测试等操作必须符合真实 B 端产品的工作流。
- 危险操作必须二次确认；表单必须有加载、错误、成功和恢复状态。
- 不使用 Tailwind、Lucide、Heroicons 或 FontAwesome。
- 不使用纯黑、紫蓝渐变、霓虹发光或卡片左侧竖线选中装饰。

## 工作方式

- 修改前先读相关 PRD 章节和现有组件。
- 每次开始新模块的布局、结构或视觉开发前，必须先输出 ASCII 页面界面，待用户确认后再改代码。
- 按模块逐一对齐：先确认该模块的信息架构、核心任务、状态和文案，再实现视觉、布局和交互。
- 每次模块优化必须优先解决信息架构和任务路径问题，再处理视觉细节；不得只做换色、加卡片或增加说明文字。
- 参考截图同时用于提炼布局、视觉样式、颜色关系、色系、字体层级、控件状态、圆角、描边、阴影、信息密度和整体风格；开发时必须基于源力设计系统 Token 与 Arco Design 重新转译，不直接复制竞品色值、代码、品牌、字段、文案或功能。
- 视觉优化工作流固定为：先读 PRD 确定页面内容和功能边界，再读参考截图提炼布局与视觉语言，再用 DESIGN.md 和源力设计系统完成 Token 转译，再用 Arco Design 实现，最后按 PRD、ASCII、视觉和组件四层验收。
- 每个模块开始开发前必须输出 ASCII、功能说明、交互说明、字段层级、状态和校验说明；得到确认后才执行。若用户明确授权夜间批量开发，可将确认粒度提升为“按已确认规则批量执行”，但仍不得突破 PRD 和设计系统边界。
- 评审标准以真实 B 端 SaaS 产品为准：界面应该让用户完成工作，而不是向用户解释界面是如何实现的。
- 只修改与当前任务直接相关的文件。
- 完成代码后运行构建和核心交互验证。
- 每次完成开发或重要验证后同步 `ROADMAP.md`。

## 一级模块统一设计基线（已确认）

后续 AI 客服、团队、技能、渠道等一级模块必须复用首页已确认的产品壳层和页面节奏，不能各自重新定义一套界面。

- 壳层：TopNav 高 48px，Sidenav 默认 200px、收起 56px；两者统一使用 `#F9FAFD`，无分割线、无独立白色顶栏、无导航投影。
- 顶部导航：左侧固定为「产品 Logo → 收起／展开侧边栏」，右侧只保留个人中心头像。
- 侧边栏：一级模块使用统一的选中态、未选中态和 Arco 图标；收起后保留图标选中态，但侧边栏右侧不得出现边框或投影。
- 内容骨架：内容区与侧边栏并排，顶部导航下方不保留额外上间距；外层延续 `#F9FAFD`，内部使用白色圆角内容容器。内容容器保留顶部和两侧描边，底部不绘制描边；收起侧边栏时内容区左侧保留 12px 壳层间距。
- 页面结构：默认采用 `PageHeader → FilterBar → Table/Card → Pagination`；标题下分隔线横跨白色内容容器通栏，标题和操作仍按内容内边距对齐。
- 组件与 Token：统一使用 Arco Design 和 `src/styles/tokens.css`；参考截图只用于布局关系、信息密度、导航结构、内容容器、选中态和页面节奏，不直接复制竞品颜色、文案、功能或组件实现。
- 业务字段：每个一级模块只展示 PRD 要求的字段；卡片或表格不得擅自增加流程、知识库、模型等摘要字段。通用卡片保持统一尺寸、等高、左对齐和响应式换行，具体字段由对应 PRD 决定。
- 交互：高频操作使用统一的悬浮／聚焦操作区、图标按钮、Dropdown、Modal／Popconfirm 和反馈消息；只保留真实任务所需文案，不添加解释产品实现的常驻说明。
- 开发门禁：新一级模块或模块结构调整，先输出 ASCII 页面界面并等待确认，再实现代码；实现后必须按本基线进行构建和核心交互验证。
- 智能体头像：新建／编辑智能体表单在底部提供头像字段，默认使用项目占位头像；头像本身是唯一上传入口，点击头像触发文件选择，不显示额外“上传头像”按钮。列表卡片直接读取智能体头像字段。
- 团队实体：团队 ID 使用纯数字字符串并在列表、详情、选择器和关联数据中保持一致；团队头像为正方形文字头像，同一团队使用稳定且可区分的彩色背景。
- 团队选择：新建／编辑 AI 客服和转人工配置复用同一团队选择组件；多选值以「彩色方形头像 + 团队名称」标签展示，支持单项移除、清空和全部选择。
