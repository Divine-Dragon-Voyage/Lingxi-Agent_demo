# 灵犀智能体 · 项目设计系统

## 1. 视觉主题与氛围

灵犀智能体是面向企业管理员、运营人员和客服负责人的客户支持智能体后台。视觉采用源力设计系统的企业级 B2B 语言：浅蓝导航壳层、白色工作区、深色正文、源力蓝交互锚点、紧凑但不拥挤的 4px 栅格。

关键词：结构化、可追踪、可信赖、信息密度高、反馈明确。

源力设计系统参考目录：`C:\Users\luxuan\Desktop\Lx_agent_demo\Volcengine Copy`。

## 2. 色彩与角色

| 角色 | Token | 值 |
|---|---|---|
| 品牌主色 | `--color-primary` | `#1664FF` |
| 主色悬停 | `--color-primary-hover` | `#0055FF` |
| 主色浅底 | `--color-primary-bg` | `#F3F7FF` |
| 页面背景 | `--color-background` | `#FFFFFF` |
| 导航壳层背景 | `--color-sidebar` | 浅蓝色系 |
| 容器背景 | `--color-surface-container` | `#F7F9FB` |
| 主文字 | `--color-foreground` | `#0C0D0E` |
| 次文字 | `--color-muted-foreground` | `#86909C` |
| 边框 | `--color-border` | `#DDE2E9` |
| 成功 | `--color-success` | `#2A814B` |
| 警告 | `--color-warning` | `#BD7E00` |
| 危险 | `--color-danger` | `#D7312A` |

限制：页面只使用一个品牌主色和语义状态色。状态色不能承担装饰功能。

## 3. 排版规则

- 中文字体：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`。
- 等宽数字：`SFMono-Regular, Consolas, "Liberation Mono", monospace`。
- 页面标题 20px / 28px，模块标题 16px / 24px，正文 14px / 22px，辅助文字 12px / 20px。
- 中文字重只使用 400、500、600，禁止斜体和伪粗体。
- 数字、时间、统计指标使用 `font-variant-numeric: tabular-nums`。
- 标题使用 `text-wrap: balance`，正文使用 `text-wrap: pretty`。

## 4. 组件样式

- TopNav：48px 高，与 Sidenav 共用同一浅蓝色系背景；两者之间不使用横向或纵向分割线，不使用独立白色顶栏或悬浮阴影。
- TopNav 与 Sidenav：视觉上属于同一连续导航壳层；TopNav 左侧按「产品 Logo → 收起／展开侧边栏」排列，右侧只保留个人中心头像。
- 顶部导航栏下方不为侧边栏或内容区保留额外顶部间距；展开或收起侧边栏时，右侧内容区的左边缘始终紧贴侧边栏右边缘。
- Sidenav：200px 宽，支持收起为图标栏；一级模块显示 AI 客服、团队、技能和渠道，AI 客服下的「客服管理」「知识库」为无图标二级菜单。选中态使用截图同类的浅灰蓝底、深灰文字与图标；未选中态保持透明背景与深灰文字。
- PageHeader：面包屑、标题、状态标签和右侧主操作。
- Button：默认 32px 高，圆角 4px；主按钮源力蓝，危险按钮语义红色。
- Input / Select：32px 高，4px 圆角，边框使用 `--color-border`。
- Table：表头浅灰，行高 48px，数字右对齐，长文本截断并提供完整内容路径。
- Tabs：优先使用线性 Tabs，选中态使用蓝色文字和底部 2px 指示线。
- StatusTag：20px 高，浅色背景 + 语义文字 + 细边框。
- Drawer：用于详情、编辑和需要参考背景的长表单。
- Modal / Popconfirm：用于发布、删除、关闭、分离等高影响操作。
- Message / Notification：用于保存、发布、上传、更新和失败反馈。
- 团队创建完成使用居中的结果 Modal：成功图标、结果标题、成员数量说明与「继续创建／完成」双操作构成单一视觉焦点；关闭弹窗等同于完成。
- 团队删除使用危险确认 Modal：先说明不可恢复与影响范围，再明确展示待删除团队名称；主操作使用危险色。内置团队不提供删除入口，被转人工配置引用的团队继续阻止删除。
- 知识文档上传使用 Arco Upload 拖拽模式：上传入口位于上方，已选文件列表位于下方；入口常驻展示 PDF 类型与单文件 50 MB 限制，列表支持逐项移除，不设置文件数量上限。
- 智能体卡片仅展示头像、智能体名称、发布状态、简介与最近更新时间；不展示流程、知识库、模型或其他详情摘要。智能体名称是卡片模块标题，使用 16px／24px、600 字重与 `--color-foreground`，超长时单行截断并在悬浮时展示完整名称。卡片采用统一固定高度，简介最多两行，避免同一行卡片出现高低不齐。悬浮或键盘聚焦时底部仅显示「配置」和纯图标「更多」。更多菜单只包含「复制智能体」和「删除智能体」，删除仍需二次确认。

## 5. 页面布局原则

标准骨架：TopNav → Sidenav + Scrollable Content；顶部导航下方，侧边栏和右侧内容区并排。内容区外层延续浅蓝导航壳层背景，页面主体为带圆角与细描边的白色容器。

- 内容区默认水平内边距 32px，窄屏降为 16px。
- 首页标题区域的文字和操作遵循内容区水平内边距，但标题下方分割线必须横跨整个白色内容容器，不在内边距处截断。
- 页面模块间距 24px 或 32px，组件内部间距使用 4px 栅格。
- 列表页：PageHeader → FilterBar → Table/Card → Pagination。智能体首页筛选仅保留左侧的名称搜索与发布状态筛选；卡片采用固定参考比例、左对齐连续排列并按可用空间自动换行，不固定一行卡片数量。
- 详情页：PageHeader → Tabs → 内容区。
- 配置页：左侧配置导航或流程列表，右侧编辑区。
- 监控页：筛选条 → 指标区 → 图表/排行 → 会话表格。
- 复杂页面优先使用分区和边框，不把每一块都包装成浮夸卡片。
- 参考截图仅用于提取布局关系、信息密度、导航结构、内容容器、选中态和页面节奏；组件、Token 和文案始终以本项目设计系统和 PRD 为准。
- 右侧页面主体使用白色背景、`--color-border` 描边和 `--radius-lg` 圆角；它从侧边栏右侧开始，与同色一体化的 TopNav／Sidenav 导航壳层形成明确层级。

## 6. 深度层级

- Level 0：白色页面背景，无阴影。
- Level 1：浅灰容器背景或细边框。
- Level 2：下拉菜单、Popover 使用 `--shadow-3`。
- Level 3：Modal、Drawer 使用 `--shadow-4`。
- 只有真正覆盖内容的浮层使用阴影，普通列表和卡片不堆阴影。

## 7. 交互状态

每个主要组件必须覆盖：default、hover、active、focus-visible、selected、loading、empty、error、disabled、read-only。

- 保存中：按钮内 loading，保持按钮尺寸不跳动。
- 保存成功：Message 短反馈，并更新最近更新时间。
- 校验失败：字段旁显示错误和修正方式，聚焦首个错误字段。
- 删除、关闭、分离、发布：二次确认并说明影响范围。
- 资源加载中：保留已有内容，使用局部 loading，不整页清空。
- 空态：说明当前状态并给出下一步操作。

## 8. 响应式行为

- 桌面端使用完整 TopNav + Sidenav。
- 低于 900px 时内容区压缩，表格允许按优先级转为卡片或横向滚动。
- 低于 768px 时侧栏收起，配置双栏变为单列或抽屉。
- 禁止页面横向滚动；长文本使用换行、截断和详情抽屉。
- 不使用 `h-screen` 锁死页面高度，使用可滚动内容区和 `min-height: 100dvh`。

## 9. Motion 哲学

- 视觉冒险度 3—5，动效强度 2—4，信息密度 6—8。
- 按钮按压 100—160ms，Popover 150—250ms，Modal/Drawer 200—300ms。
- 只对 transform、opacity 和明确的颜色属性做过渡。
- 不使用 `transition: all`、`ease-in`、`scale(0)` 入场。
- 高频键盘操作不加动画。
- 必须提供 `prefers-reduced-motion` 降级。

## 10. Arco Design 转译规则

源力设计系统的静态 HTML 是视觉和结构参考，不直接复制到 React。React 组件使用 Arco Design 实现，但必须保留源力的 Token、尺寸、状态和布局语义。

| 源力组件 | React 实现 |
|---|---|
| TopNav / Sidenav | 自定义布局组件 + Arco Design 图标或本地 SVG |
| PageHeader | `PageHeader` 自定义组件 |
| Button | Arco Design `Button` + 项目 Token |
| Table | Arco Design `Table` |
| Form / Input / Select | Arco Design `Form`、`Input`、`Select` |
| Tabs | Arco Design `Tabs` |
| StatusTag | 自定义 `StatusTag` |
| Drawer / Modal | Arco Design `Drawer`、`Modal` |
| Message / Alert | Arco Design `Message`、`Alert` |

原系统的 32px 控件高度、4px 栅格、源力蓝、浅灰侧栏和页面骨架必须保留。原系统中的静态 class、业务 Logo 和无关图标不直接带入。

## 11. 真实产品文案与 Demo 边界

本项目是按真实 B 端 SaaS 项目方式开发的前端产品演示 Demo，不是静态产品原型，也不是产品使用说明页。

- Demo 只模拟后端数据、请求结果和状态变化；页面结构、交互路径、组件行为和文案遵循真实产品习惯。
- 文案只保留任务所需的标题、字段、状态、操作、校验、反馈和必要的业务说明。
- 不在页面中解释内部实现、数据关系、演示目的或组件行为；例如“关闭或删除技能会同步解除智能体绑定关系”不作为常驻说明文案。
- 只有当用户即将做出高影响决策，且该影响不能从操作结果或确认弹窗中自然理解时，才补充影响范围说明。
- 任何新增说明文案都必须回答一个具体决策问题，否则不进入界面。

## 12. 首页壳层微调（已确认）

- TopNav 与 Sidenav 的统一背景色固定为 `#F9FAFD`。
- 展开侧边栏时，内容区与侧边栏并排；收起为图标栏后，内容区左侧保留 12px 壳层留白，避免选中菜单贴住内容容器。
- 白色内容容器保留上方与两侧浅描边；底部不绘制浅描边。收起侧边栏本身不绘制右侧描边或投影。

## 13. 一级模块统一设计基线（已确认）

首页不是单页特例，而是灵犀智能体所有一级模块的共同界面基线。AI 客服、团队、技能和渠道页面必须复用以下结构与视觉规则：

- TopNav／Sidenav 使用同一 `#F9FAFD` 背景，TopNav 高 48px，Sidenav 默认 200px、收起 56px；不使用导航分割线或投影。
- 顶部左侧固定为「产品 Logo → 收起／展开侧边栏」，右侧只保留个人中心头像；一级侧边栏统一使用 Arco 图标、选中态和未选中态。
- 收起侧边栏后保留图标选中态，但 Sidenav 右侧不保留边框或投影；内容区左侧保留 12px 壳层间距。
- 内容区与侧边栏并排，TopNav 下方不留额外上间距；外层背景为 `#F9FAFD`，内部为白色圆角内容容器。内容容器保留顶部与两侧描边，底部不绘制描边。
- 一级列表页优先采用 `PageHeader → FilterBar → Table/Card → Pagination`；标题下分隔线通栏，标题和操作按内容内边距对齐。
- Arco Design 负责组件实现，源力 Token 负责颜色、字体、间距、圆角、尺寸和状态。参考竞品截图只提取布局关系、信息密度、导航结构、内容容器、选中态和页面节奏。
- PRD 决定每个模块的字段和功能边界；页面不得因复用参考布局而增加 PRD 未要求的字段、功能或说明文案。通用卡片和表格只复用结构、尺寸、等高、对齐、悬浮操作与状态表现，业务字段按模块 PRD 定义。
- 所有一级模块保持同一套页面节奏和交互反馈：筛选、空状态、加载、错误、成功、危险操作确认、悬浮操作和 Dropdown 均遵循统一组件行为。
- 新一级模块或结构调整必须先提交 ASCII 界面并获得确认，再进入实现；完成后需更新 ROADMAP 并执行构建与核心交互验证。
- 智能体头像字段位于新建／编辑表单底部；默认显示项目占位头像，点击头像触发上传并通过悬浮提示和无障碍标签表达交互，不增加常驻说明文案。一级模块智能体卡片使用该字段展示头像。
- AI 客服与人工客服头像统一使用圆形；团队头像继续使用方形，以轮廓区分客服实体与团队实体。AI 客服详情页头像右下角叠加轻量编辑图标，整枚头像仍是唯一上传入口。
- 团队列表沿用一级模块的白色圆角内容容器、通栏标题栏、搜索区和表格节奏；团队头像使用正方形文字头像，并按团队稳定映射不同彩色背景。
- 团队 ID 为纯数字字符串；列表、详情、AI 客服团队绑定和转人工目标团队必须使用同一 ID。
- 团队详情成员表格只展示成员身份信息与移除操作，不展示接待优先级或其他路由配置；移除该字段后，成员名称列自然扩展，操作列保持右对齐。
- 团队选择器统一使用 Arco Select 转译：已选项展示彩色方形头像与团队名称，支持移除、清空；多选下拉提供「全部选择」并显示选中状态。

## 14. 智能体详情页框架（已确认）

- 详情页采用「顶部操作栏 → 左侧配置导航 → 中间配置区 → 右侧预览与测试」四层结构。
- 顶部操作栏展示返回、可点击的智能体名称、发布状态、最近保存时间、测试和发布；点击名称打开「编辑智能体」弹窗。
- 左侧导航分为「配置」和「监控」两组，默认选中「智能体」；监控项仅出现在详情页导航中，不上升为一级模块。
- 右侧预览与测试面板使用浅蓝色背景，其余详情页区域使用白色背景和浅色分割线。
- 「智能体」配置包含基础信息、人设、记忆和异常回复；基础信息复用创建智能体时的名称、简介和头像数据。
- 详情页配置不使用「保存配置」按钮，表单修改采用防抖自动保存，顶部最近保存时间随保存成功更新；发布仍是独立动作。
- 记忆开关与「记忆」标题同一行；记忆有效期使用「自定义／永久有效」切换，自定义展示 1—365 天滑杆，历史会话数展示 1—30 滑杆且不附加单位。
- 详情页是独立全屏工作区，不复用首页 TopNav 和一级 Sidenav；顶部只保留详情页操作栏，详情导航只存在于页面内部。
- 基础信息不出现在「智能体」配置内容中，仅通过点击顶部智能体名称打开「编辑智能体」弹窗维护。
- 顶部不显示「测试」按钮；预览与测试固定在所有详情配置页面最右侧，仅提供轻量对话测试，不展示旧测试 Drawer 的命中调试内容。
- 中间配置区与右侧预览区之间使用可拖拽分隔线，鼠标按住分隔线可自由调节右侧宽度，并设置最小／最大宽度。
- 详情页 Input、TextArea、Select、Picker 等控件默认使用白底细描边；仅禁用态使用浅色背景，聚焦态使用源力蓝描边。
- 记忆有效期的「永久有效／自定义」使用浅灰色圆角分段容器；选中项使用白色圆角背景和源力蓝文字，未选中项保持灰色文字。
- 记忆关闭时只保留「记忆」标题和开关，记忆有效期、有效天数和历史会话数全部收起；重新开启后恢复原配置。
- 记忆模块下「记忆有效期」与「历史会话数」是同等级配置块；「有效天数」仅属于「记忆有效期」。
- 右侧预览与测试面板在详情页工作区内占满可用高度，消息区弹性填充，输入框固定在面板底部。

## 15. 视觉参考转译工作流（已确认）

参考截图不只用于布局，也用于提炼视觉样式、颜色关系、色系、字体层级、控件状态、圆角、描边、阴影、信息密度和整体风格。

固定执行顺序：

1. PRD：确定页面、功能、字段、业务流程、状态、校验和交互边界。
2. 参考截图：提炼布局关系、视觉语言、颜色关系、信息密度和交互节奏；不带入竞品品牌、字段、文案、功能和代码。
3. 源力设计系统：将提炼出的视觉语言映射为项目 Token，保证全项目统一。
4. Arco Design：使用项目组件完成输入、选择、Tabs、滑杆、弹窗、反馈和状态交互。
5. 验收：按 PRD、已确认 ASCII、视觉系统和组件行为四层检查。

每个模块开发前必须先输出 ASCII、功能说明、交互说明、字段层级、状态和校验说明，并等待确认。只有用户明确授权批量执行时，才可以按已确认规则连续开发多个模块。
